---
description: R-DOC-01〜11 — doc-lane 自律資料作成（PPTX/DOCX/経営会議）— kintone deploy 混在禁止
globs:
  - docs/runbooks/doc-lane*.md
  - docs/runbooks/keiei-kaigi*.md
  - docs/plans/*doc-lane*
  - templates/doc-lane/**
  - scripts/build-*-report*.py
  - scripts/verify-doc-lane*.mjs
alwaysApply: false
---

# doc-lane ゲート（R-DOC — 2026-06-27 浜田 GO）

**承認**: `docs/approved-changes/2026-06-27-rules-doc-autonomous-hamada-go.md`  
**正本**: `docs/runbooks/doc-lane-autonomous-governance.md`

## レーン（R-DOC-01）

- 資料作成（PPTX / DOCX / 資料 / マニュアル / フロー図）着手時は **`[doc-lane]` 1 行宣言**
- **kintone deploy / customize / フィールド deploy と同一ターンで混在禁止**

## 着手前（R-DOC-02）

**PPTX（MCP）**:

```powershell
npm run verify:doc-lane-governance
npm run verify:doc-lane-pptx-phase1
npm run health-check
```

**DOCX（MCP / セキュリティレポート）**:

```powershell
npm run verify:doc-lane-keiei-kaigi   # 経営会議セキュリティ（R-DOC-11）
npm run verify:doc-lane-governance
npm run verify:doc-lane-word-phase2
npm run health-check
```

**v5 Word**: `npm run cio:doc-lane-gate -- --strict`

いずれも **NG → 作業中止**。

Skill: `.cursor/skills/office-docx-doc-lane/SKILL.md`（DOCX）/ `office-pptx-doc-lane`（PPTX）/ `kintone-doc-lane`（全体）

## 環境（R-DOC-03）

- **PPTX / Word MCP** = **Windows Cursor 必須**（WSL のみ ⏭ なら中止）
- 出力 = **`C:\tmp\`**（registry 準拠）— **完成バイナリを Git commit しない**（R-DOC-04）

## 編集（R-DOC-05 / R-DOC-06）

- 既存ファイル → **backup コピー後**に編集
- PPTX グラフ: スライド幅 **10 in** 前提 — `left+width ≤ 9.5`
- 保存後 **read-back** 失敗 → 再保存しない

## 完了（R-DOC-07）

機械 verify だけで「完成」にしない。報告末尾に **浜田目視依頼 1 行**（`doc-lane-completion-report.md`）。

人事・社外・公式セキュリティ資料 → **§50-3-8 DeepSeek 必須**（R-DOC-08）。

**経営会議セキュリティ（R-DOC-11 / R7）**: §1 = 浜田ネタで AI 起稿。§2・社外事例 = **枠のみ**（数値・事例は浜田入力）。正本 `keiei-kaigi-security-report.md`。

## MCP 選択（R-DOC-09 / R-DOC-12）

| Cursor UI / mcp.json | AI 内部呼び出し |
|----------------------|-----------------|
| `office-powerpoint` | `user-office-powerpoint` |
| `office-word` | `user-office-word` |

- 新規 PPTX + 図解 → **`user-office-powerpoint`**
- 月次セキュリティ（R7）→ **前月 DOCX copy + `office-word` MCP**（§1 AI / §2 枠）— builder は JSON 全文時のみ（R-DOC-14）
- 複雑フロー → **figma** `generate_diagram` → **`add_picture`**
- 既存 PPTX 精密パッチ → **`pptx-patch-windows.md`**（python-pptx）

`npm run cio:tool:route -- --intent "…"` で doc-lane intent を確認。
