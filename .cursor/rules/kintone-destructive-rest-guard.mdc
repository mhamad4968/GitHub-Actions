---
description: kintone REST の DELETE・全件入替・本番アプリ破壊級の前に dry-run＋CEO GO（2026-05-06 浜田承認）
alwaysApply: true
---

# kintone 破壊系 REST — 実行ゲート（CIO 必読）

**正本の補足**: `AGENTS.md` **§41**（データ破壊境界）・`chat-sessions/desktop-ai-emergency-read-pack/18-SESSION-ONE-REPORT-2026-05-06.md` §2〜§6。

## 1. 対象（このルールがかかる操作）

- **`DELETE /k/v1/records.json`**（レコード削除・一括 id 削除）
- **全件入れ替え**に相当する **大量 POST/PUT**（既存を実質無効化するもの）
- **アプリ設定の破壊**（フィールド削除・本番アプリ削除に近い preview 操作など）

## 2. 実行順（省略禁止）

1. **dry-run または既定安全モード**で **削除・変更対象の id・見出し・件数**を取得し、**チャットにそのまま貼る**（ログだけに閉じない）。
2. **ユーザー（浜田 CEO）から「この一覧で削除 GO」**または同等の **明示 GO** があるまで、**本番 apply（`--apply` 等）を走らせない**。
3. **可能なら先に UI だけ**（例: customize で非表示・フィルタ）を行い、**物理削除は GO 後の別コミット／別シェル**に分ける。

## 3. スクリプト設計（リポに追加する破壊系 npm）

- **既定は dry-run のみ**（対象表示で終了）。
- **本番削除は `--apply` が無いと実行しない**（`scripts/pc-ledger-quick-guide-prune-meta-chapters.mjs` が正本例）。

## 4. 憲法との接続

- **§35-1**: コマンド実行は AI でもよいが、**破壊の最終 GO は浜田**。
- **§50-3-8**: 破壊系を含むタスクでは **着手前に DeepSeek 等 1 問**、または **`§50-3-8 スキップ理由:`** 1 行。
