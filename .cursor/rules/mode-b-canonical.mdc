---
description: 方式B・固定4AI — 用語・四行テンプレ・役割の単一窓（AI読み込み最適化・2026-05-29）
alwaysApply: false
globs: "**/*"
---

# 方式B — 固定4AI（AI-KERNEL）

> **上位**: `AGENTS.md` §1-2-3-4 / §1-2-3-4-A / §1-2-3-4-B / §1-2-3-4-C / §50-3-11  
> **他 `.mdc`**: 四行テンプレを**コピー禁止** — 本ファイル参照のみ。

## 空間認識図（1秒）

```
  ┌─────────┐    §50-3-8     ┌───────────┐   eslint+tree  ┌──────┐
  │ ① CIO   │ ─────────────► │④ DeepSeek │ ──────────────► │② Comp│
  │ Opus4.7 │ ◄───────────── │  盲点3点  │                 │ 2.5  │
  └────┬────┘    突合3行     └───────────┘                 └──┬───┘
       │                                                        │
       │  1行要約 ◄── ③ Kimi レビュー ◄─────────────────────────┘
       ▼
    CEO（GO・目視）

  ═══ 15ターン壁 ═══  turn>15 & !export → exit 1 → cio:session:export-handoff
```

```mermaid
flowchart LR
  CIO["① CIO Opus4.7/4.8"] --> DS["④ DeepSeek §50-3-8"]
  DS --> CMP["② Composer 2.5"]
  CMP --> KM["③ Kimi review"]
  KM --> CIO
  WALL{{"15ターン / 40k wall"}} --> EXP["export-handoff.json"]
  EXP --> NC["New Chat --import"]
  NC --> MAP["4AI引っ越しマップ"]
  CMP -->|verify×2 fail| DS2["DeepSeek Self-Heal"]
  DS2 -->|3 fail| CIO2["CIO→CEO"]
```

## 前提条件

| 項目 | 内容 |
|------|------|
| 体制 | 固定4AI（CIO / Composer / Kimi / DeepSeek） |
| CIO | Opus **4.7 ベース** — 大局判断・深検証のみ **4.8**（§1-2-3-4-B） |
| 非置換 | §50-3-8 / §50-3-11 / §35-1 / §1-2-2 — **削除・弱体化禁止** |
| 画像 | GenerateImage + `assets/images/` のみ — **画像 MCP 計画削除** |

## 4AI マトリクス（表記ゆれ禁止）

| # | 役割 | 担当 | ◯ | ✕ |
|---|------|------|---|---|
| ① | CIO | Opus 4.7/4.8 | 指揮・承認 | 大量 Diff 直接実務 |
| ② | コード | Composer 2.5 Subagent | Diff・SPEC | §50-3-8 なし単独 save/deploy/PUT |
| ③ | 長文 | Kimi | 精査・`kimi_review` | Diff 適用・指揮 |
| ④ | 知恵袋 | DeepSeek | §50-3-8・盲点3点 | 指揮代行・長文主筆・コード編集 |

**4.8 切替 🎖️**: `CIO=統合判断(Claude Opus 4.8適用)` — 詳細: `docs/runbooks/cio-opus48-intelligence-activation.md`

## 実行手順

### 連携プロトコル（順序固定）

1. CIO — 方針 + 毎ターン4行（🎖️）
2. DeepSeek — §50-3-8 盲点3点
3. Composer — Diff・実装
4. Kimi — レビュー → CIO — CEO へ1行要約

### 仕様触れ前（§50-3-11）

1. DeepSeek 1問 → 2. 突合3行 → 3. `npm run cio:guard:5038 -- --stamp`
4. **Composer MCP 監査** — eslint-mcp + repo-tree → `npm run cio:guard:composer-mcp-audit -- --stamp`（`composer-mcp-audit-gate.mdc`）

### 実装ゲート

- 80行超 or `customize/**` → **Composer Subagent のみ**
- 金曜: `docs/runbooks/cio-friday-mcp-status-refresh-4ai.md`
- Opus 4.8 重ターン: `docs/runbooks/cio-opus48-intelligence-activation.md`

### 命令圧縮（Opus 4.8 / Composer 向け・§1-2-3-4-C）

- 箇条書き + 表優先 — 散文・重複禁止
- 1 指示 = **前提 / 手順 / 禁止 / exit 判定** の4要素
- 全文 Read 禁止 — **Diff・offset/limit** のみ

## 禁止事項

- Composer **単独** GO なし save/deploy（CIO または DeepSeek 経由後のみ）
- 四行テンプレの他 `.mdc` へのコピー
- Token Bloat（不要な全文 Read）
- verify ループ **4回以上**（ゾンブループ）
- 画像 MCP 導入

## 判定コード（exit 0/1）

| コマンド | 合格 |
|----------|------|
| `npm run verify:cio-four-ai-governance` | exit 0 |
| `npm run verify:mode-b-turn-head-canonical` | exit 0 |
| `npm run cio:guard:5038` | exit 0 or `--skip` 理由 |
| `npm run verify:cio-mcp-registry` | exit 0 |
| `npm run cio:session:turn-guard -- --check` | exit 0 |
| `npm run verify:session-handoff-integrity -- --import` | exit 0（New Chat 時） |
| `npm run cio:composer:escalation-guard -- --check` | exit 0 |
| `npm run cio:task:score-spec` | exit 0 |
| `npm run verify:cio-autonomous-infra` | exit 0 |
| `npm run verify:cio-env-integrity` | exit 0 |
| `npm run cio:error:generate-ticket` | exit 0（Self-Heal 上限時） |
| `npm run verify:cio-environment-infra` | exit 0 |
| `npm run cio:error:apply-ticket-choice -- --choice N` | exit 0 |
| `npm run cio:env:self-healing` | exit 0 |
| `npm run verify:cio-extreme-defence-infra` | exit 0 |
| `npm run cio:rollback:weekend-actions` | exit 0（週明け救済） |
| `npm run verify:cio-spec-logic` | exit 0 |
| `npm run verify:cio-final-closure-infra` | exit 0 |
| `npm run verify:constitution-genre-kernels` | exit 0 |
| `npm run verify:desktop-ai-emergency-sync` | exit 0 |

## 毎ターン先頭4行（機械検査一致・正本）

```text
[§1-2-3 ティア判定: L2] <根拠1語>
【適用憲法】§35-1 §50-3-11 §1-2-3-4-B 第10層
[🎖️ 本セッション割当] CIO=Opus4.8 | Composer=未使用 | DeepSeek=未使用 | Kimi=未使用
[ルール確認] mode-b-canonical.mdc + docs/constitution/19-governance-four-ai-kernel.md
```

- **L1** = Composer / **L2** = CIO 4.7ベース / **L3** = Opus 4.8 深検証（方式Bハイブリッド）

## 詳細（人間・監査用）

完全マトリクス全文: `AGENTS.md` §1-2-3-4-A（**非置換**）。手順: `docs/runbooks/cio-four-ai-governance.md`
