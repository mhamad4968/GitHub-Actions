# 東海支店 iPad 管理台帳 — kintone 仕様書（SPEC 草案）

> **起票**: 2026-07-28 (火)  
> **状態**: **実装完了（M1–M6）— 浜田目視待ち（M7）** — 浜田実装 GO 2026-07-28  
> **配置**: [Space 32](https://jbis-kintone.cybozu.com/k/#/space/32) / thread **34**  
> **アプリ**: DB **[769](https://jbis-kintone.cybozu.com/k/769/)** / Dash **[770](https://jbis-kintone.cybozu.com/k/770/)**  
> **移行元 Excel**: `C:\tmp\東海支店iPad管理台帳\iPad管理台帳（26.7～）.xlsx`（Sheet1・**25 台**・**移行済**）  
> **UI 参照**: [JR iPad 管理台帳 721](https://jbis-kintone.cybozu.com/k/721/) 型（DB＋Dash・有効/廃棄トグル・A4 印刷）  
> **境界**: JR iPad **720/721 は closed-v1・触らない**。本案件は **別新設**。  
> **同期中継**: `docs/runbooks/tokai-ipad-sync-relay.md` / Desktop `東海支店iPad_同期リレー_START.bat`

---

## §0. 進め方

1. **本書で項目・運用・フィールドを確定**する（**Q1–Q19 済 — 2026-07-28**）。
2. **commit / push** → 浜田 **実装 GO**。
3. GO 後: Space 32 に DB＋Dash 作成（`creation-timing-ask.mdc`）→ フィールド → Excel 25 台移行 → customize → 裏側同期 API → 浜田目視。
4. 着手前に **§50-3-8** 再実行。

**技術方針**（720/721・714/715 型）:

| 構成 | 役割 |
|------|------|
| **データアプリ（DB）** | 1 端末 = 1 レコードの正本（**東海支店iPad台帳DB**） |
| **ダッシュアプリ** | 日常 UI（**東海支店iPad管理台帳**） |
| **有償プラグイン** | **NG** |
| **DB 標準 UI** | **だれも触れない**（保存・削除・編集を customize で全面禁止。書込は Dash REST のみ） |
| **操作入り口** | **管理台帳（Dash）のみ** |

**CIO 既定（ブロッカーなし・浜田未問のまま進めてよい）**:

| 項目 | 既定 |
|------|------|
| 初期構築 | AI が API でアプリ作成・フィールド・移行。その後 DB UI ロック |
| 同期失敗 | Dash にエラー表示＋再同期（保存再実行） |
| Space thread | 作成時に Space 32 の実 thread を選定し `kintone-apps.md` に記録 |
| 開始日 Excel シリアル | 移行スクリプトで日付型へ変換 |

---

## §1. 背景・目的

### 1.1 背景

- 東海支店系 iPad を Excel で管理。kintone 化したい。
- UI は 721 イメージ。項目は異なる。M365／VPN も載せたい。
- **tokai** と **admin** のみ本台帳を利用。**tokai に PC台帳 674・社員マスタ 595 の画面アクセスは付けない**。

### 1.2 目的

1. Excel 相当の台帳を kintone に置き、日常操作は Dash のみ。
2. 新規端末名 `tokai`＋連番の自動採番（廃棄後は欠番）。
3. 保存時に裏側 API で 595／674 から必要情報をコピー（Lookup 不使用）。
4. Excel 出力・印刷（一覧／ユーザー単位）。印刷は 721 型 A4・端末別。

### 1.3 スコープ

| 含む | 含まない |
|------|----------|
| Space 32・DB＋Dash・25 台移行 | JR 720/721 の改修 |
| tokai 編集（追加・所属変更・廃棄） | Lookup フィールドによる 595/674 直結 |
| 有効／廃棄トグル | 有償プラグイン |
| M365/VPN（ID・PW）同期・グレーアウト | tokai への 595/674 権限付与 |

### 1.4 管理対象拠点（5）

東海支店／東京営業所／静岡営業所／名古屋営業所／関西営業所

---

## §2. 権限

| 主体 | Dash | DB UI | 595 / 674 画面 |
|------|------|-------|----------------|
| **tokai** | 閲覧・編集（追加・変更・廃棄） | **不可** | **不可** |
| **admin** | 全操作 | **不可**（運用中） | 従来どおり |
| **その他** | 不可 | 不可 | — |

初期構築・移行・フィールド変更は **AI（管理者 API トークン）** が実施。

---

## §3. 他台帳との境界・同期

| 対象 | 役割 | 本台帳との関係 |
|------|------|----------------|
| **社員マスタ 595** | 所属・利用者の正本 | Dash 検索・保存時コピー（**裏側 API**）。tokai は画面不可 |
| **PC台帳 674** | M365 / VPN の正本 | 保存時に氏名キーで `m365_id`/`m365_pw`/`vpn_id`/`vpn_pw` をコピー。tokai は画面不可 |
| **JR iPad 720/721** | 別ドメイン | **触らない** |

### 3.1 同期タイミング

- **保存ボタン押下時**（Dash → 裏側 API → DB 更新）。
- M365／VPN 欄は **グレーアウト・手入力不可**（同期結果のみ表示）。

### 3.2 紐づけキー

- **氏名（利用者）**。
- 同姓同名はほぼ無い前提。発生時は **浜田に事前相談**（自動推定しない）。

### 3.2a 674 複数ヒット時の AI 判定（2026-07-28 追記）

氏名一致が複数でも、**廃棄／取消は現行ではない**と AI チームが判断してよい。

| 順 | ルール |
|----|--------|
| 1 | `pc_status` が **廃棄** または **取消** の行は除外 |
| 2 | 残りに **利用中** があれば **利用中のみ**に絞る |
| 3 | 候補が **1 件** → 自動採用（`pc_ledger_record_id` もその `$id`） |
| 4 | 候補が **0 件** → NO_HIT（空保存は確認ダイアログ） |
| 5 | 候補が **2 件以上**（例: 利用中が2台）→ **MULTI_HIT・浜田相談**（自動採用しない） |

正本実装: `scripts/lib/tokai-ipad-kintone.mjs` の `resolvePcLedgerCredentialHits`（CLI・中継・Dash 共通）。

### 3.3 検索 UX

- 所属・利用者は **検索で入力**（裏側 API が 595 を検索し候補表示）。

---

## §4. アプリ構成・名称

| 役割 | アプリ名（案） | 備考 |
|------|----------------|------|
| DB | **東海支店iPad台帳DB** | 正本・UI 操作禁止 |
| Dash | **東海支店iPad管理台帳** | 日常入り口 |

---

## §5. ステータス

| 値 | 意味 |
|----|------|
| **有効** | 運用中（一覧の既定表示） |
| **廃棄** | 解約・使用停止（レコード残す・欠番） |

- 一覧: 既定＝**有効のみ**。ボタンで **廃棄のみ**表示（721 型）。
- **物理削除はしない**（誤登録救済が必要な場合のみ admin＋AI で別途対応・原則廃棄）。

---

## §6. フィールド定義（DB＝正本）

| # | ラベル | コード（案） | 型 | 必須 | 備考 |
|---|--------|--------------|----|------|------|
| 1 | 拠点 | `location` | ドロップダウン | ○ | 5 拠点固定 |
| 2 | 利用者 | `user_name` | 文字列1行 | ○ | 595 検索 |
| 3 | 端末名 | `device_name` | 文字列1行 | ○ | `tokai`＋連番・一意 |
| 4 | レンタル開始日 | `rental_start_date` | 日付 | — | Excel シリアルは移行時変換 |
| 5 | 機種 | `model` | 文字列1行 | — | |
| 6 | 端末認識電話番号 | `phone_number` | 文字列1行 | — | |
| 7 | IMEI | `imei` | 文字列1行 | — | |
| 8 | ICCID | `iccid` | 文字列1行 | — | |
| 9 | Appleシリアル番号 | `apple_serial` | 文字列1行 | — | |
| 10 | 共有パスコード | `shared_passcode` | 文字列1行 | — | 印刷・Excel に含む |
| 11 | M365 ID | `m365_id` | 文字列1行 | — | 同期・グレーアウト |
| 12 | M365 パスワード | `m365_pw` | 文字列1行 | — | 同期・グレーアウト・印刷/Excel 含む |
| 13 | VPN ID | `vpn_id` | 文字列1行 | — | 同期・グレーアウト |
| 14 | VPN パスワード | `vpn_pw` | 文字列1行 | — | 同期・グレーアウト・印刷/Excel 含む |
| 15 | ステータス | `status` | ラジオ/DD | ○ | 有効／廃棄 |
| 16 | 595レコード番号 | `employee_record_id` | 数値 | — | 検索選択時に保持（任意） |
| 17 | 674レコード番号 | `pc_ledger_record_id` | 数値 | — | 同期ヒット時（任意） |

---

## §7. 採番

- 新規: **`tokai` + ゼロ埋め連番**（現行 Excel は `tokai01`〜`tokai25`。桁は既存に合わせ **2 桁以上**・実装時に `tokaiNN` 最大＋1）。
- 廃棄後: **再利用しない（欠番）**。
- 次番号は **有効＋廃棄を含む全端末名**の最大連番＋1。

---

## §8. Excel 移行

| 項目 | 内容 |
|------|------|
| 件数 | **25 台** |
| 変換 | 開始日のシリアル値 → 日付 |
| M365/VPN | Excel 空欄は空のまま移行。初回保存または一括同期ジョブで 674 から埋める |
| ステータス | 全件 **有効** |

移行後、Excel は運用終了（kintone 正本）。

---

## §9. Dash UI

| 機能 | 内容 |
|------|------|
| 一覧 | 721 型。有効／廃棄トグル |
| 新規 | 次 `tokaiNN` 表示＋作成 |
| 編集 | 所属変更・項目編集。保存時に 595/674 同期 |
| 廃棄 | ステータス＝廃棄（欠番） |
| 検索入力 | 所属・利用者（595・裏側 API） |
| M365/VPN | 表示のみ（disabled） |
| Excel 出力 | **一覧**／**ユーザー単位**（PW・共有パスコード含む） |
| 印刷 | **一覧**／**ユーザー単位**。A4・端末別（721 型）。PW・共有パスコード含む |
| 集計 | **拠点別台数**（利用＝有効／廃棄／合計）。行クリックで拠点＋有効に絞込 |

---

## §10. 裏側 API（保存時同期）

1. Dash 保存 → カスタマイズが **中継エンドポイント**（管理者トークン）を呼ぶ。  
2. `user_name` で **595** を検索 → `location` / `user_name` を確定（検索選択済みなら ID 優先）。  
3. 同氏名で **674** を検索 → `m365_id` / `m365_pw` / `vpn_id` / `vpn_pw` をコピー。  
4. ヒットなし／複数ヒット（同姓同名）→ エラー表示。複数時は **浜田相談**（自動採用しない）。  
5. ブラウザに 595/674 のトークンを埋め込まない。

実装置き場は実装 GO 後に `scripts/` または既存中継パターンへ決定（SPEC 非ブロッカー）。

---

## §11. 浜田確認一覧（§41）

| # | 内容 | 結果 |
|---|------|------|
| Q1 | M365/VPN＝API コピー同期 | ✅ |
| Q2 | tokai 編集可 | ✅ |
| Q3 | 廃棄ステータス＋一覧トグル | ✅ |
| Q4 | DB＋Dash | ✅ |
| Q5 | 紐づけ＝氏名 | ✅ |
| Q6 | PW も台帳掲載 | ✅ |
| Q7 | tokai に PW 可視 | ✅ |
| Q8 | `tokai`＋連番採番 | ✅ |
| Q9 | 保存時裏側同期・M365/VPN グレーアウト | ✅ |
| Q10 | Excel 列＋ステータスで十分＋Excel/印刷（一覧・ユーザー） | ✅ |
| Q11 | 印刷＝721 型 A4 | ✅ |
| Q12 | 25 台初回移行 | ✅ |
| Q14 | 印刷・Excel に PW・共有パスコード含む | ✅ |
| Q15 | 674 から M365/VPN のみ | ✅ |
| Q16 | 595＝所属・氏名／674＝M365・VPN | ✅ |
| Q17 | 595 検索 API・DB はだれも触れない | ✅ |
| Q18 | 廃棄後欠番 | ✅ |
| Q19 | 同姓同名は稀・事前相談 | ✅ |

---

## §12. マイルストーン（実装 GO 後）

| # | 内容 |
|---|------|
| M1 | Space 32 に DB＋Dash 作成・権限（tokai/admin） |
| M2 | フィールド投入・DB UI ロック customize |
| M3 | Excel 25 台移行 |
| M4 | Dash UI（一覧・採番・廃棄トグル・検索） |
| M5 | 保存時同期 API（595/674） |
| M6 | Excel 出力・印刷 |
| M7 | 浜田目視・Excel 運用終了 |

---

## §13. 改定履歴

| 日付 | 内容 |
|------|------|
| 2026-07-28 | 草案。浜田 Q1–Q19・AIチーム突合反映 |
| 2026-07-28 | 実装 GO。Space32 に DB **769** / Dash **770** 作成・ACL・DB UI lock・Excel 25 台移行。次は M4 Dash UI |
| 2026-07-28 | M4–M6 完了。Dash v1 deploy・同期中継・674→M365/VPN 23/25 反映。残ギャップ: tokai20 複数ヒット・tokai22 未ヒット |
| 2026-07-28 | §3.2a: 674 複数ヒット時は廃棄/取消除外・利用中優先を AI 判定可に。tokai20=272 反映済 |
| 2026-07-28 | Dash v3: 拠点別台数集計（有効/廃棄/合計・行クリック絞込） |
