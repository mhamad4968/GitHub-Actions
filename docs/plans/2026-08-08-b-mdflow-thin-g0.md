# B-MDFLOW 薄い版（G0）— 2026-08-08

> **地位**: 調査1ページのみ（**build しない**）  
> **スコア**: B-MDFLOW（P21）— フルは L・9月以降可  
> **触らない**: 新フレームワーク導入・予実専用 MD フローの全面置換

## 1. いま足りているもの（既存）

```
仕様 md ──► 実装 code ──► レビュー checklist
   │              │                │
   docs/plans     customize/**     §50-3-8 / verify:*
   SPEC.md        scripts/**       Desktop 報告
```

| 既存正本 | 役割 |
|----------|------|
| §50-3-2a / SPEC 系 | 仕様タッチ時の第2者 |
| `docs/plans/*` | 依頼・計画の md 正本 |
| `cio:request:compose` | 依頼文の機械化（①で強化済） |
| `cio:tool:route` | 実装前の入口固定 |
| closures / checkpoint | 閉済・次の1手 |

## 2. 足りない（薄いギャップ）

| ギャップ | 影響 | 今やる？ |
|----------|------|----------|
| 「仕様=このmd」の1枚依存図が散在 | 新人CIOが迷う | **本G0で依存図のみ** |
| score が constraint を work 扱い | 空月の誤誘導 | checkpoint で緩和済 · コード化は任意 |
| 予実専用フローの一般化 | 工数 L | **9月以降** |

## 3. 結論

**既存の §50 / plans / compose / route で「薄い MD フロー」は既に回っている。**  
フル B-MDFLOW（一本化エンジン）は作らない。次に GO するなら「依存図を Desktop 早見に1枚足す」程度で足りる。

## 4. フル実装を今やらない理由と、やれる境界（2026-08-08 追記）

| 層 | 内容 | 今 |
|----|------|-----|
| L0 薄い | 依存図（本G0） | **済** |
| L1 | Desktop 早見1枚（仕様=md / 実装=code / 検証=verify） | **任意・GO後** |
| L2 フル | 予実専用フローの lab 全体一般化・新ツール | **9月以降・L工数** |

**やれるところ**: L0 の依存図を計画に固定した。L1 は浜田が「Desktopに足して」と言ったときだけ。
