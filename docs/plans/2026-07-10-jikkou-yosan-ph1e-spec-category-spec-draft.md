# 736 PH1e — 仕様明細（①）区分列（工事費 / 保安費）

**日付**: 2026-07-10 JST  
**状態**: **GO**（浜田 2026-07-10 — 保存は警告のみ・保存 OK）  
**親 SPEC**: [`2026-06-18-jikkou-yosan-spec.md`](2026-06-18-jikkou-yosan-spec.md) §9  
**live**: [App 736](https://jbis-kintone.cybozu.com/k/736/)

---

## 1. 依頼

仕様明細（①）の **仕様の右**に **区分** 列（DROP_DOWN: **工事費** / **保安費**）。  
**合計 …①** の下に **工事費合計**・**保安費合計**。未選択行は内訳に計上せず、赤文字で **`区分が未選択の行があります（不明 ¥X）`**（未選択 0 件なら非表示）。

## 2. 集計

| 区分 | 工事費合計 | 保安費合計 | 合計 …① |
|------|-----------|-----------|---------|
| 工事費 | ✅ | — | ✅ |
| 保安費 | — | ✅ | ✅ |
| 未選択 | — | — | ✅ |

`合計 …①` = 全行 `spec_amount` 合計（従来どおり）。  
`工事費合計 + 保安費合計 + 不明合計 = 合計 …①`（金額が入っている行）。

## 3. kintone

- サブテーブル `spec_lines` に **`spec_category`**（DROP_DOWN: 工事費 / 保安費 / 空）

## 4. 既存データ

- 読込時 `spec_category` 空かつ `spec_name` が **「保安費」で始まる** → **保安費** を UI 状態にセット（保存時に永続化可）
- それ以外の空行 → **空のまま**（赤警告）

## 5. 保存

- 未選択行があっても **保存ブロックなし**（警告のみ）

## 6. UI 追補（同日）

- フッター **％** は金額列の右隣セル（備考列位置）· 分母 = 合計 …①
- **deploy**: `2026-07-10-736-spec-category-ph1e` rev175 → `-fix` rev176（`recalcState` が `spec_total_*` を state に未コピー）→ `-pct` rev177

## 7. 実装メモ

`recalcAll` で追加した集計フィールドは **`recalcState` で `state` に必ずコピー**（PH1f の `cost_budget_*` / `profit_budget_*` も同様）
