# 2026-04-18 (Sat) 計画メモ：SKYSEA未導入PC自動インストール

## ⚡ 開始予定: 2026-04-19 07:00 JST

> 朝 7 時から着手予定。`docs/reports/2026-04-19-morning-prep.md` の「⚡ 時刻指定タスク」セクションで本タスクが最優先表示される（#R3）。

- 起票: 2026-04-17 19:24 (Fri) — 翌日着手予定の事前計画
- 要望（原文）:
  > 明日はSKYSEAクライアントが入っていないPCを探してAD等で自動的にインストールさせる仕組みをつくりたい。条件はPCが立ち上がっている間に入れたい。

---

## 1. ゴール（暫定）

1. **SKYSEAクライアントが導入されていないPCを自動的に発見する**
2. **発見したPCに、起動中のまま自動インストールさせる**
3. 結果（成功 / 失敗 / 端末名 / 利用者）を可視化（kintone PC台帳との突合）

> 「PC起動中に入れたい」 = 再起動・ログオフを待たず、**現在オンラインの端末**にプッシュ配信したい

---

## 2. アーキテクチャ候補（明日朝にユーザーへ確認したい）

### 候補A: SKYSEA 自身の「リモートインストール」機能を活用
- SKYSEA 管理機側に「未受信端末リスト」と「リモートインストール」機能あり（Sky 標準）
- 一覧→選択→配布が一番素直で安全
- 必要: 管理機の権限・現状の運用ルール確認

### 候補B: AD (Active Directory) GPO ソフトウェアインストール
- コンピュータ構成 → ソフトウェアインストール（MSI 割り当て）
- 既定では **次回起動時** 適用 → 「起動中に入れたい」要件にミスマッチ
- ログオンスクリプト方式にすればオンライン中も実行可だが、ログオン待ちが発生

### 候補C: スクリプト＋WinRM (PowerShell Remoting)
- 管理サーバから `Invoke-Command -ComputerName <PC> -FilePath install-skysea.ps1`
- **起動中の端末に即時配信可能**（要件に合致）
- 必要: ドメイン管理者権限 / WinRM(5985/5986) 開放 / Defender ASR 例外
- インストール元: 共有フォルダ `\\fileserver\skysea$\SkySetup.msi` 等

### 候補D: PsExec
- `psexec \\PC -s -d msiexec /i \\share\SkySetup.msi /qn`
- 軽量だが Defender がブロックすることが増えている

### 候補E: Intune / SCCM
- 既存導入があるなら最も筋が良い。なければ今回は対象外

---

## 3. PC在席状況の把握（kintone PC台帳との突合）

| 必要データ | 取得元 | 突合キー |
|---|---|---|
| 全PC一覧 | kintone 594 (PC台帳) | `PC_name`（=Windowsホスト名 想定） |
| SKYSEA導入済み端末 | SKYSEA 管理機エクスポート | ホスト名 |
| AD コンピュータ一覧 | `Get-ADComputer` | Name |
| 現在オンラインの端末 | AD `LastLogonDate` / `Test-Connection` | ホスト名 |

突合フロー (案):
```
[594 PC台帳全件] - [SKYSEA導入済CSV] = [未導入PC候補]
        ↓
[AD `Get-ADComputer -Filter *`]
        ↓
[ping/Test-NetConnection で「いま起きてるPC」を抽出]
        ↓
[Invoke-Command で SkySetup.msi をプッシュインストール]
        ↓
[結果を CSV → 594 へ「最終インストール試行日」フィールド追記]
```

---

## 4. 明日朝に確認させてください（ヒアリング項目）

1. **SKYSEA管理機**から「未導入端末リスト」を CSV 出力できますか？（管理機の操作可否・出力フォーマット）
2. SKYSEA の **リモートインストール機能（候補A）** を使えばよろしいですか？それとも独自スクリプト配布が必要？
3. **Active Directory** は使われていますか？ ドメイン名・管理者権限の有無
4. PCの**ホスト名は kintone 594 の `PC_name` と完全一致**していますか？（ハイフン/末尾年月の違いに注意）
5. **WinRM が有効**になっていますか？（GPO で `winrm quickconfig` 配信済か）
6. **インストール用 MSI** はどこに置かれていますか？（共有パス・サイレント引数）
7. 結果は**kintone 594 にフィールド追加して書き戻す**運用でよいですか？（例: `skysea_installed_at`, `skysea_status`）
8. **失敗時の通知**は？（例: メール、kintone通知、何もしない）
9. 対象外にすべきPC（**サーバ・JR端末・廃却済み**等）はありますか？
10. **本番環境でテスト可能なPC**を1〜2台用意していただけますか？（まずは少数台で検証したい）

---

## 5. 事前にこちらで進められる準備（ユーザー回答前にできること）

- [x] kintone 594 から「現役PC」の PC_name 一覧を抽出する集計スクリプトの雛形（即実装可） → **2026-04-19 着手済み**: `scripts/skysea-recon.mjs` で実装・実行完了。SKYSEA × 594 突合の 4 CSV を `data/skysea/` に出力（installed-pcs / already-installed / needs-install / orphan-in-skysea）
- [ ] PowerShell スクリプト雛形（突合 → 起動確認 → リモートインストール → 結果CSV） → **2026-04-25/26 持ち越し**
- [ ] kintone 594 にフィールド追加案： → **2026-04-25/26 持ち越し**
  - `skysea_status` (DROP_DOWN: 未確認 / 導入済 / 未導入 / 失敗 / 対象外)
  - `skysea_checked_at` (日時)
  - `skysea_install_log` (複数行テキスト)
- [ ] 「📌 SKYSEA未導入」トグルを 594 検索パネルに追加する設計案（既存トグル群と同じ枠で） → **2026-04-25/26 持ち越し**

---

## 6. リスク・注意点（先に共有しておきたい）

- **WinRM プッシュインストール**は AV/EDR から「ラテラルムーブメント」として誤検知されやすい。事前に Defender ASR 除外申請が必要なことが多い。
- **再起動を伴うインストール**は業務中だと事故になるため、SKYSEA は無人インストールでも「**バックグラウンドで完了 → 次回起動時にサービス開始**」のオプション運用が安全。
- **失敗ログ収集**を最初から仕込むこと（端末名・時刻・MSI終了コード）。
- 既に kintone 側には **PC1台に複数アカウント** や **同一 ledger_record_id 重複** があり、PC_name の一意性の前提に注意。

---

## 7. 明日の進め方（提案）

1. (5分) 上記ヒアリング 10 項目を一気に確認
2. (15分) 候補A〜Eのいずれにするかを決定
3. (30分) PoC: 1台に対する手動実行で動作確認
4. (60分) 突合スクリプト＋自動配信スクリプトの実装
5. (30分) kintone 594 へのフィールド追加＋トグル UI
6. (任意) 結果ダッシュボード化

---

参考情報（こちらで一次把握済み）:
- SKYSEA Client View Windowsサービス名は通常 `SKYSEAClientView`
- インストールパス: `C:\Program Files\Sky Product\SKYSEA Client View\`
- 起動中/未導入の判定はサービス存在＆プロセス `SkyService.exe` の有無で確認可能

---

> ユーザーへ：この計画でよろしければ明日着手します。
> 修正・優先度変更があればその旨、朝にお知らせください。
> まずは**ヒアリング10項目への回答**だけ準備いただければ、すぐ走り出せます。

---

## 進捗（2026-04-19 追記）

### 実施済み（朝のセッション）

- ✅ `scripts/skysea-recon.mjs` 実行 → `data/skysea/` に 4 CSV 出力（08:27）
  - `installed-pcs-2026-04-19.csv`（158 行・SKYSEA 元データ）
  - `already-installed-2026-04-19.csv`（122 行・両方あり = 正常）
  - `needs-install-2026-04-19.csv`（136 行・kintone あり / SKYSEA なし = 🔴 要インストール）
  - `orphan-in-skysea-2026-04-19.csv`（32 行・SKYSEA あり / kintone なし = 🟡 削除候補）
- ✅ ライセンス充足判定: 保有 241 / 使用中 158 / 残 83 → 要 136 で **不足 53**（追加発注 2 週間）
- ✅ orphan 32 件の中身分析: 個人 PC 廃却漏れ + **共有 PC** + **管理用 / 研修用** + **🚫サーバ・NAS** が混在していることを発見

### 中断要因と新ゴール

- 朝のチャットでユーザーと削除候補について対話中、ポリシーブロックでチャット途絶 → 新セッションで AI が文脈喪失
- ユーザー判断で **SKYSEA 完了は来週土日（2026-04-25/26）に持ち越し**、本日は「継続性の喪失」を構造的に解消することが最優先に再設定
- TSB-005「セッション間継続性の構造的脆弱性」を `docs/troubleshooting.md` に記録

### 来週土日（2026-04-25/26）にやること

1. **`scripts/skysea-recon.mjs` を再実行**（最新版で 8 列出力 = `prefix` `推定原因` 列追加版）→ orphan CSV を最新化
2. orphan を **4 カテゴリに仕分け**:
   - ⭕ **個人 PC 廃却漏れ**（KS / JBIS で始まる + 個人として現役）→ 削除候補
   - ⚠ **共有 PC**（東京支店共有①、5F 会議室共用 等）→ 個人台帳にないのが正常 / 共有 PC 台帳側を要確認
   - ⚠ **管理用 / 研修用**（hashiba 管理用端末、研修用 等）→ 同上
   - 🚫 **サーバ・NAS**（DS-02 Docushere、KentnasMain、KENT-AD-001 AD）→ **削除厳禁**
3. ユーザー（浜田）と削除可否を 1 件ずつ確認（自動削除はしない）
4. plan §4 ヒアリング 10 項目で **自動インストール仕組みの方針決定**（候補 A〜E のいずれか）
5. PowerShell 雛形・kintone 594 フィールド追加・「📌 SKYSEA 未導入」トグル UI 設計

### §49 として、来週着手前に必ず確認すべきこと

- **直近起動が新しい PC は現役稼働中**（例: `KS0297-202312` 湯川さん 2026-04-18 / `KS0282-202311` 米沢さん 2026-04-17）。SKYSEA から消す前に**台帳側に登録すべきか**を確認
- **`KS00235-202304`**（5 桁番号）など命名規則からはみ出すレコードは、命名揺れか別物か要判別
- **plan §6 リスク**（PC1 台に複数アカウント / 同一 ledger_record_id 重複）が orphan 仕分けに影響する可能性あり
