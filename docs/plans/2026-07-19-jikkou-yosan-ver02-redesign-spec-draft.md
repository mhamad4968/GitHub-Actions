# 実行予算書 Ver.02 再設計仕様（ドラフト）

**作成日**: 2026-07-19  
**状態**: **DRAFT / 総括 CLOSED／内訳 CLOSED（2026-07-20 昼・AI最終合意）／次=予実（夜）→版管理／実装 GO なし**  
**対象**: 実行予算書 Ver.02 の要件・データモデル・試作受入条件  
**非対象**: 実装、kintone 変更、build、deploy、Ver.01 改修

## 1. 本書の位置づけ

本書は、受領した新ワークブックを基に Ver.02 を再設計するための仕様たたき台である。
既存 Ver.01 の仕様・資産は参考にするが、新ワークブックと 2026-07-17 以降の決定を優先する。

本書内の判断状態を次の3種類に限定して明示する。

| 状態 | 意味 |
|---|---|
| **CONFIRMED** | 浜田決定、依頼者決定、または観測事実として確定 |
| **PROVISIONAL** | 読める資料からの仮説。依頼者確認前は実装根拠にしない |
| **OPEN** | 未決。2026-07-20 以降に1問ずつ確認する |

本書の作成自体は、実装開始、アプリ変更、build、deploy の許可を意味しない。

## 2. 参照正本

- [実行予算書システム仕様正本（Ver.01）](2026-06-18-jikkou-yosan-spec.md)
- [版管理・差分表示仕様](2026-06-20-jikkou-yosan-version-management-spec.md)
- [Phase 1 フィールド辞書](2026-06-18-jikkou-yosan-field-dictionary-phase1.md)
- [Phase 2 計算仕様・サンプル検算](2026-06-18-jikkou-yosan-phase2-calculation-spec.md)
- [2026-07-17 夜反省](../reports/2026-07-17-evening-reflection.md)

参照正本と本書が衝突する場合、Ver.02 については本書の **CONFIRMED** を優先する。
ただし **PROVISIONAL** と **OPEN** は既存確定事項を自動的に上書きしない。

## 3. 受領ワークブックと読解上の制約

### 3.1 受領事実 — CONFIRMED

| 項目 | 内容 |
|---|---|
| ファイル | `C:\tmp\実行予算ver2\工事予算（実行予算）(案) (1).xlsx` |
| シート | `コード表`、`総括`、`内訳`、`データマスタ` の4シート |
| 検査結果 | `#REF!` を含む数式が **563件** |

### 3.2 要件抽出ルール — CONFIRMED

1. **基本は受領 Excel を正**とする（浜田 2026-07-20・§15.0）。
2. ワークブックのレイアウト、見出し、保存済み表示値、読み取れる数式を主要な要件資料とする。
3. `#REF!` が多数あるため、元の計算式を完全・正確に復元できたとは主張しない。
4. 読めない参照を補って導いた計算、分類、マスタ用途はすべて **PROVISIONAL** ／原則 **OPEN（依頼者確認）** とする。
5. 推測事項は本文だけに埋めず、依頼者確認リストへ明記する。
6. 本書では、提示された事実を超える具体的なワークブック金額・数式を創作しない。
7. Ver.01 と Excel が衝突する場合は **読める Excel を優先**し、衝突点は依頼者確認リストへ。

## 4. 2026-07-17 の方針と現行保護

### 4.1 Ver.01 維持 — CONFIRMED

- 現行 **App 736 Ver.01** は保存し、Ver.02 のために上書き・改造しない。
- マスタは現行 **App 735**。
- 現在信頼できる本番識別は
  **BUILD `2026-07-12-736-ui-backlog-02-col-resize` / rev186**。
- 2026-07-17 の決定は「新ワークブック確認後に Ver.02 のみ再設計」である。
- 既存文書には rev185 等の記載もあり、ファイルキー記録にも衝突がある。
  したがって、将来 deploy 前にアプリID、revision、BUILD、ファイルキーの同一性を再照合する。

### 4.2 do not touch / deploy — CONFIRMED

- App 736 Ver.01 rev186 を変更しない。
- App 735 のフィールド、レコード、権限を変更しない。
- 736 の customize、設定、レコードを Ver.02 試験に流用しない。
- build、deploy、スキーマ変更、データ移行、テストレコード作成を行わない。
- Ver.02 試作先は **Space 56・アプリ名「実行予算書作成支援ツールver02」**（§15.8）。**App ID 未採番。作成は実装 GO 後。**
- 識別情報の衝突を解消し、浜田から明示的な実装 GO を受けるまで deploy／アプリ作成しない。

## 5. Ver.02 の目的と優先順位

### 5.1 目的 — CONFIRMED

新ワークブックの業務構造に合わせ、予算の入力、内訳、総括、実績消費、残予算、
版履歴・差分を一貫して追跡できる実行予算管理を設計する。

### 5.2 試作優先順位 — CONFIRMED

最優先は見た目の完成度ではなく、次の連鎖の正確性と追跡可能性である。

`入力 → 内訳 → 総括 → 実績 → 残予算`

- 入力値の出所を追えること。
- 集計先と集計根拠を追えること。
- 予算版と実績の関係を追えること。
- 修正前後を差分として説明できること。
- 仮説と確定要件を混同しないこと。

## 6. 画面構成

### 6.1 4タブ — CONFIRMED

| タブ | 主目的 |
|---|---|
| **総括表** | 工事基本情報と、売上・原価・粗利を含む全体集計 |
| **内訳** | 工種別の予算明細入力・表示と、総括・予実への予算供給 |
| **予実管理** | 実績入力、予算対比、残予算、消化率の確認 |
| **バージョン管理** | 版一覧、複製、確定、比較、差分確認 |

表示順、タブ内の列順、色、固定領域は **PROVISIONAL** とし、機能確認後に調整する。

## 7. 総括表

### 7.1 再利用する概念・資産 — CONFIRMED

現行 App 736 から、次のうち新ワークブックと整合するものを再利用候補とする。

- 工事コード、工事名称、発注者、期間、担当者等の工事基本情報。
- **売上①、原価⑧、粗利⑨** の総括概念。
- 明細を版間で突合する安定した行キー。
- 行追加・削除・並び替え等の行操作。
- マスタ検索・選択の考え方。
- 版管理、直前版・当初版・選択版との比較、差分表示。

再利用はコードの無条件コピーを意味しない。新ワークブックに適合するかを項目単位で確認する。

### 7.0 工事基本情報 — CONFIRMED（方針・浜田 2026-07-20）

[App 736](https://jbis-kintone.cybozu.com/k/736/) の基本情報パネル（見やすさ・項目構成）を **ベース**にし、Excel総括にあって736に無い項目を **追加**する。736のコードを無条件移植はしない（レイアウト・項目の踏襲）。

| 736にある（踏襲） | Excelにあって736に無い／不足気味 |
|---|---|
| 工事コード・正式名称・工事名称・発注者 | **工期日数**（Excel `AC4`=280。着手〜しゅん功から算出） |
| 着手日・竣工日・現場入場予定日・立案日 | **工事コード枝番**（Excel `F5`−`M5` 例: `2423101-2`）— **必要（CONFIRMED）** |
| 桁種別・発注支社・部門・安衛則88条 | |
| 作成者・担当者・備考・版メタ | |

**日数**: 着手日・竣工日から **自動計算・表示**（手入力しない）。式は Excel正 **`AC4=U5-U4`（竣工−着手の暦日差）** — **CONFIRMED**（浜田 2026-07-20）。未入力時は空白。

**工事コード枝番（浜田 2026-07-20 CONFIRMED）**: 基本情報に本体コード＋枝番。枝番は **任意**。空なら `2423101` のみ、ありなら `2423101-2` 表示。版番号（`version_seq`）とは別概念。

### 7.1a 請負金額（①の明細）— CONFIRMED（浜田 2026-07-20・Excel正）

総括上部の **請負金額** ブロックが①の内訳である。

| 項目 | Excel | 内容 |
|---|---|---|
| 見出し | `A10` | 請負金額 |
| 施工明細 | `B10`=`施工` 配下（契約工種・数量・単価・金額税抜） | 例: けた橋ペイント・足場工 等 |
| 保安明細 | `B20`=`保安` 配下 | 例: 線閉責任者・列車見張員 等 |
| 施工計 | `AE19` | `SUM` 施工側金額 |
| 保安計 | `AE33` | `SUM` 保安側金額 |
| **① 合計** | `AE34 = SUM(AE19,AE33)`、ラベル `…①` | **施工計＋保安計** |

- 請負金額は **原価行（⑧）とは別**。給与手当は請負金額側には出ない（原価側のみ）。
- 金額は税抜・整数円（§15.3）。数量×単価の式は Excel で健全（`V*Z`）。
- **行操作（浜田 2026-07-20）**: 施工側・保安側とも **追加・削除可**。初期は各1行。削除後も **施工・保安それぞれ最低1行は残す**。合計①は常に施工計＋保安計を再計算する。
- **単位**: 共通DD（下記 CONFIRMED）。
- **数量**: 入力・表示可（桁の強制丸めはしない・B5）。サンプルに `1639.6` 等。金額は整数円（端数は §15.3／B2。ただし **X1 矛盾あり** → §7.5）。

#### 請負金額・単位候補（Excel観測 → 浜田確認）

| 候補 | 請負金額での実在 | 内訳等での実在 | 備考 |
|---|---|---|---|
| ㎡ | ○ | ○ | Excel表記は `㎡`（`m2` ではない） |
| 式 | ○ | ○（最多） | |
| 回 | ○ | ○ | 保安側に多い |
| 人 | — | ○ | 労務系。請負でも使うなら候補に含める |
| 日 | — | ○ | |
| 缶 / 枚 | — | ○ | 材料系。請負に要るかは判断 |
| ％ / － | — | ○ | 計算行・空欄用。請負では不要寄り |

**単位候補 — CONFIRMED（浜田 2026-07-20）**: 請負・原価・給与で共通ドロップダウン **`㎡` / `式` / `回` / `人` / `日` / `箇月` / `－`**。足りなければ候補追加。給与手当行の初期単位は **`箇月`**（Excel正）。**内訳**はこれに加え **`缶` / `枚` / `％`**（U16）。

- **未入力表示（浜田 2026-07-20）**: **契約工種に入力したあと**、その行の **単位・数量・単価** が未入力なら当該セルを **薄い赤** 等で明示する。契約工種が空の初期行は赤にしない。備考は未入力でも赤にしない。
- **セル種別色（浜田 2026-07-20・U21）**: 請負・総括も **自動／手入力／リスト**のごく薄い3色を適用（詳細は §8.5 U21）。薄い赤は不足指摘が優先。
- **備考（浜田 2026-07-20）**: **フリー入力**（任意・文字制限は通常の1行／複数行テキスト範囲）。
- **金額（浜田 2026-07-20・Excel正 `AE=V*Z`／X1解消）**: **常に数量×単価の自動計算**（金額の手入力・上書きなし）。**契約工種が未入力の行**は金額欄を **空白**。合算は金額が入った行のみ。基幹と1円差がある場合は **作成者が数量・単価を調整**して合わせる（運用）。
- **契約工種（浜田 2026-07-20）**: 当面は **手入力（フリー）**。ドロップダウン／マスタ化は必要になった時点で別途相談（本試作の前提に含めない）。
- **金額列・請負（浜田 2026-07-20 CONFIRMED）**: **金額（税抜）のみ**。消費税列・税込列は置かない。①・施工計・保安計の正は税抜。
- **金額列・原価（浜田 2026-07-20 CONFIRMED）**: Excelどおり **消費税**列を残し、**金額（税抜）と金額（税込）の両方**を置く。税込は行の消費税率に応じ一般的計算（例: 税抜×(1+税率)、整数円。端数は R-02）。⑧の正は税抜合計のまま（税込は併記）。
- **消費税率（浜田 2026-07-20 CONFIRMED）**: 表示はドロップダウン **`0％` / `8％` / `10％`**。内部値は **`0` / `0.08` / `0.1`**。税込＝税抜×(1+内部値)。**新規行の初期値は 10％**。正本は **総括の原価行**（X4・Excel正）。内訳は税抜積み上げ。給与は `－`（X7）。
- **備考列（浜田 2026-07-20 CONFIRMED）**: **請負**は **備考のみ**（1列・フリー）。**原価行**は Excel の `計算基準・備考`（`AR37`）を **計算基準** と **備考** の **2列に分割**する（いずれも任意・フリー入力可）。

### 7.4 総括レビュー（2026-07-20）— **CLOSED（再OPEN解消後）**

矛盾深掘り（§7.5）を経て浜田確認完了。AIチーム最終合意（Sequential Thinking＋Composer。DeepSeek/Kimi MCPは当該セッション未接続）により **総括ラウンド CLOSED**。次セッションは **内訳**（昼）→ 予実・版管理（夜）。

#### A. 改善・方針（CONFIRMED／仮置き）

| ID | 内容 | 状態 |
|---|---|---|
| Imp-01 | **編集境界**: 施工・保安の数量・単価・金額の正は **内訳**。総括側は集計表示＋種別／消費税／計算基準／備考等（**総括限定・非逆流** D-45）。**給与手当は内訳なし＝数字も総括が正**（例外） | **CONFIRMED** |
| Imp-02 | **率の定義一本化**: 対①率＝`金額÷①`（小数1位）。①＝0なら率0。Excel `1−…` は使わない | **CONFIRMED**（Q12） |
| Imp-07 | 計のUIラベルを請負／原価で区別（X5） | **CONFIRMED** |
| Imp-03 | 請負・給与の行並び替えは初回試作外（追加削除のみ） | 仮置き |
| Imp-04 | 給与の役職名は手入力 | 仮置き |
| Imp-05 | 着手＞竣工は保存時警告（自動入替はしない） | 仮置き |
| Imp-06 | Excel「【最終】」「別表」等の印刷メタは試作外 | 仮置き |

#### B. 追加質問への確定（浜田 2026-07-20）

| # | 決定 |
|---|---|
| **B1** | 原価（施工・保安）の数字は **内訳で変更**する。総括単独編集は整合が保てない |
| **B2** | （X2で精緻化）金額は ROUND なしの積。表示は整数。四捨五入注記は基幹／運用。1円差は作成者が数量・単価調整（X1） |
| **B3** | 工事コード **枝番は基本情報に必要**（例 `2423101-2`） |
| **B4** | 未入力の薄い赤があっても **警告は出すが保存可能** |
| **B5** | 入力した数量等は **そのまま保持**（入力値自体を端数処理しない）。金額計算時の丸めは B2 |

#### C. 内訳・予実・バージョンへ委譲（総括矛盾解消後）

`#REF!` リンク先、工種番号空行、ブロック初期セット、消化率詳細、版権限・監査、App 678 連携。  
**単位混在（Q8 CONFIRMED）**: 混在／合計ブロック → 総括は `式`×`1`×税抜合計。同一セル複数参照（`AQ$88` 等）→ **U15 CONFIRMED**（単一ソース・金額不変）。

### 7.5 総括・矛盾／疑問の深掘り（AIチーム 2026-07-20・再レビュー）

DeepSeek/Kimi MCP未接続のため Sequential Thinking＋Composer＋Excel再突合。結論: **総括はまだ OK ではない**。以下を解消してからクローズする。

#### 矛盾（仕様同士・Excelとの衝突）

| ID | 内容 | 深刻度 | 提案 |
|---|---|---|---|
| **X1** | 「金額は自動計算」と B2「1円狂い時はシステム数値を入力」 | **解消** | **CONFIRMED（浜田 2026-07-20）**: 金額は常に自動計算（上書きUIなし）。Excel注記は作成者運用。1円差は数量・単価を直して合わせる |
| **X2** | 端数の計算手順 | **解消（浜田＋Excel正 2026-07-20）** | Excelは `=V*Z` 等で **ROUNDなし**。数量は端数あり可。金額は計算結果をそのまま持ち、**小数点表記なし（整数表示）**。B2の「小数第1位四捨五入」は **基幹システム側の話／作成者運用**であり、Excel式・kintone計算式に ROUND を入れない |
| **X3** | B1「原価は内訳で変更」と給与（内訳なし）の関係が文面で曖昧だった | **中** | **給与の数字は総括が正**と明記（Imp-01更新済）。追加確認不要なら確定 |
| **X4** | 消費税率の正本 | **解消** | **CONFIRMED（浜田・Excel正）**: **A** — 総括の原価行で設定。内訳は税抜の積み上げ |
| **X5** | 「施工計」「保安計」が請負①と原価⑧で同名別物 | **解消** | **CONFIRMED（浜田）**: 請負側＝請負・施工計／請負・保安計／①。原価側＝原価・施工計／原価・保安計／給与計／⑧ |
| **X6** | ドラフト内の古い記述の自己矛盾 | **進行中** | 主要矛盾は解消済。残る仮置きは Imp-03〜06（並び替え初回外等） |
| **X7** | 給与の消費税＝`－` のとき税込列 | **解消** | **CONFIRMED（浜田）**: 税込も **`－` 表示**（税抜同額にはしない） |

#### 疑問・抜け（矛盾未満だが未決）

| ID | 内容 |
|---|---|
| Q8 | 単位混在時の総括投影 | **解消** | **CONFIRMED（浜田）**: 内訳で単位混在（または合計算出）のブロックは、総括で **単位 `式`・数量 `1`・単価＝税抜合計**（編集不可）。金額整合が本体 |
| Q9 | 種別・計算基準・備考の逆流 | **解消** | **CONFIRMED（浜田・Excel正）**: **総括限定**（内訳へ書き戻さない） |
| Q10 | 枝番の必須／表示 | **解消** | **CONFIRMED（浜田）**: 枝番は任意。空＝本体のみ、あり＝`本体-枝番` 表示。一意キー詳細（＋版）は版管理ラウンドで詰める |
| Q11 | 新規時の請負行数 | **解消** | **CONFIRMED（浜田）**: 新規は施工1行・保安1行。追加／取込で増やす |
| Q12 | サマリー率 | **解消** | **CONFIRMED（浜田）**: 率＝`金額÷①`（小数1位）。Excelの `1−…` 式は使わない。**①＝0 のときは率 `0`**（「—」ではない） |
| Q13 | 薄い赤の適用範囲 | **解消** | **CONFIRMED（浜田）**: 請負と同型。名称系入力後に単位・数量・単価が空なら薄い赤。施工・保安は内訳投影済みなら通常赤にならない。種別・備考・計算基準は赤対象外 |
| Q14 | 基本情報の必須 | **解消** | **CONFIRMED（浜田）**: [App 736](https://jbis-kintone.cybozu.com/k/736/) と同じ。必須は **工事コード**・**版種別**・**安衛則88条**（既定「有」）。枝番は任意（Q10）。その他ヘッダは任意 |

#### Excel再突合メモ（根拠）

- 請負金額: `AE=V*Z`、①=`SUM(AE19,AE33)` — 健全
- 原価単価: `AH38=内訳!$AQ$30` 型（多く `#REF!`）— **B1（内訳が正）と整合**
- 給与: `AH84` が直値、`AM=AD*AH` — **総括が正と整合**
- 工期日数: `AC4=U5-U4` — 確定済
- B2注記の原文セルは受領ブックから **未検出**（浜田記憶／別資料の可能性）

#### AIチーム最終合意（2026-07-20）

| 項目 | 結果 |
|---|---|
| 合意 | **総括ラウンド CLOSED** で一致 |
| 参加 | Sequential Thinking＋Composer（DeepSeek/Kimi MCPは未接続のため代替） |
| 残リスク（総括外） | 内訳の同一合計複数参照（`AQ$88` 等）、`#REF!`、ブロック初期セット |
| 仮置き維持 | Imp-03〜06（並び替え初回外・給与役職手入力・日付逆転警告・印刷メタ試作外） |
| 次 | 内訳 → 予実 → 版管理。実装・deploy は浜田 GO 後 |

### 7.2 工事原価の区分（施工・保安・給与手当）— CONFIRMED（構造観測） / 明細帰属は OPEN

総括の原価行は、次の **3区分** が `工事原価額及び率…⑧` に合算される構造である（浜田指摘・セル観測 2026-07-20）:

| 区分 | 総括見出し | 行帯（観測） | 小計行 |
|---|---|---|---|
| **施工** | `B38`=`施工` | 概ね 38–74 | `C75`=`計` → `AM75` |
| **保安** | `B76`=`保安` | 概ね 76–82 | `C83`=`計` → `AM83` |
| **給与手当** | `B84`=`給与手当` | 概ね 84–88 | `C89`=`計` → `AM89` |

- `AM90`（⑧）の式は `=SUM(AM75,AM83,AM89)`（施工計＋保安計＋給与手当計）— **CONFIRMED（読める数式）**。
- 各明細がどの区分に属するか・またがる場合の扱いは **OPEN（依頼者確認）**。
- 初回試作の予実集計も、この3区分を追跡できることを目標とする（旧「施工/保安の2区分のみ」記述は本節で上書き）。

### 7.3 計算権威 — CONFIRMED（進め方） / ①⑧⑨関係 CONFIRMED / 構成要素は OPEN

進め方は **§15.0**。①⑧⑨の意味は **§15.2**（浜田確定）:

- **①**＝売上側合計、**⑧**＝工事原価合計、**⑨**＝粗利（①−⑧）

総括ラベル（`…①` / `上記項目の合計値…⑧` / `①-⑧…⑨`）と一致する。
①＝請負（施工＋保安）、⑧＝原価（施工＋保安＋給与）、⑨＝①−⑧は **CONFIRMED**。
税・丸め方針は **§15.3／B2**。ただし計算手順の細部は **§7.5 X1・X2** が未解消。

## 8. 内訳

### 8.0 内訳ラウンドの進め方 — CONFIRMED（浜田 2026-07-20 昼）

総括ラウンドと同型。§15.0 を内訳でも再確認する。

1. **基本は受領 Excel を正**（読めるレイアウト・見出し・保存値・健全な数式）。
2. **Excel「正」の範囲**: 読める構造・区分・連番・健全式の結果。`#REF!`・意味不明は正としない（断定禁止）。
3. **進行形**: 論点ごとに **観測事実 → 提案（A/B）→ 浜田確認**。確認待ちで全体を止めない。
4. **依頼者確認の要否**: AIチーム提案＋**浜田と都度相談**して決める（独断で依頼者リストへ載せない／載せ忘れもしない）。
5. 途中でも追加の意見・懸念を出してよい（浜田・AI双方）。
6. 実装・deploy・App作成は引き続き **GO 後**。

### 8.1 構造 — CONFIRMED（浜田 2026-07-20・Excel正＋Ver.01詳細表UX）

- 内訳は **工種ごとにブロック分割して表示**する（Excel `内訳` シートと同型）。
- 各明細は安定した行キーを持ち、版複製後も同じ行の同一性を維持する。
- 内訳は総括表の予算集計元となる。
- 内訳は予実管理の予算集計元となる。
- 内訳から工種別かつ **施工 / 保安 / 給与手当** の3区分別に集計できる構造とする。

### 8.2 工種ブロックの追加と総括連携 — CONFIRMED（浜田 2026-07-20）

現行 App 736 詳細表（[live](https://jbis-kintone.cybozu.com/k/736/)）の「外注ブロックを追加 → 総括に連携行」と同趣旨で、Ver.02 は次を満たす。

| 要件 | 内容 | 状態 |
|---|---|---|
| ブロック追加 | 内訳タブで工種ブロックを追加できる | **CONFIRMED** |
| 総括へ反映 | 追加したブロックは総括の原価行（該当区分内）に行／集計として追加・反映できる | **CONFIRMED** |
| Excel正 | ブロック内の列・小計・諸経費等は受領 Excel の読める構造を正とする | **CONFIRMED**（§15.0） |
| 初期セット | 初期表示するブロック一覧 | **CONFIRMED**: 新規は **ブロック 0**（作成者が追加）。Excel No.1–42 は候補／見本。強制42投入なし（U1・U9） |
| ブロック削除時 | 総括側の対応行も **連動削除**（現行736同型） | **CONFIRMED**（浜田 2026-07-20） |
| 給与手当の行追加・削除 | 総括の **給与手当** 区分で **追加・削除可**。**内訳ブロックなし**（総括直入力）。単位初期 `箇月`。消費税は **`－`**。**税込も `－`**（X7）。削除後も **最低1行は残す** | **CONFIRMED**（浜田 2026-07-20） |
| 総括フッター・区分別サマリー | 画面最下部に [App 736](https://jbis-kintone.cybozu.com/k/736/) 同趣旨で **①・⑧・⑨を主表示**。直下に原価・施工計／保安計／給与計を小さめ併記。各行に **対①の率**（`金額÷①`・小数1位）。**①＝0 のとき率は `0`** | **CONFIRMED**（浜田 2026-07-20・Q12） |
| 施工・保安の総括単独行操作 | **不要**。増減は内訳ブロックの追加／削除に連動して総括へ自動反映のみ | **CONFIRMED**（浜田 2026-07-20） |
| 工種番号・システム入力工種 | **コード表**（システム工種コード／名称）から選択・自動反映 | **CONFIRMED**（浜田 2026-07-20・Excel構造） |
| 種別（総括 N 列） | **手入力可**。コード表からは取らない。将来DD化する場合は依頼者からリスト受領後に別途 | **CONFIRMED**（浜田 2026-07-20） |

参照: Ver.01 詳細表のブロック追加UI・総括連携（緑行）・PH1d／UI-BACKLOG-03。Ver.02 は工種ブロック単位に一般化する。

### 8.3 内訳№（No.n）— CONFIRMED（方針） / 詰め直しは OPEN

Excel 観測:

| 場所 | 内容 |
|---|---|
| 総括 `AZ37` | 見出し `内訳№` |
| 総括 `AZ38`〜 | `No.1` …（内訳と連携する原価行） |
| 内訳 `BE` | 各工種ブロック先頭に同番号（Excel正・右端） |
| 給与手当行 | 内訳№なし |

**CONFIRMED（浜田 2026-07-20）**:

1. 内訳ブロックは表示順に **No.1 から連番**し、総括の内訳№と対応させる。
2. **給与手当は総括だけで行追加**（内訳ブロックなし・内訳№なし）。
3. 版間同一性は **安定ブロックID**、内訳№は表示・並び用。
4. **表示位置（案A）**: 内訳画面では **システム入力工種の左**に自動表示（Excelの `BE` 右端は踏襲しない。見やすさ優先）。色は自動＝ごく薄い青（U21）。

**OPEN**: ~~並び替え時に番号を詰め直すか（欠番なし）／欠番を残すか。~~ → **U14 CONFIRMED（詰め直す）**  
**CONFIRMED（浜田 2026-07-20）**: 内訳ブロック削除時は総括の対応行も **連動削除**。削除後の内訳№は **欠番なく詰め直す**（暫定・Excel連番と整合）。

### 8.4 ブロック境界・列 — PROVISIONAL / 依頼者確認

ブロックの見出し、入力列、計算行、小計行、マスタ選択項目はワークブックから抽出する。
`#REF!` により参照関係が隠れている箇所の境界・集計対象は依頼者確認を必須とする。

### 8.5 内訳ブロック・明細行の入力UX — CONFIRMED（浜田 2026-07-20 昼）

Excel正＋作成者の入力負荷低減。請負（総括）の「契約工種＝当面フリー」とは別レーン（内訳のシステム工種・明細列）。

| # | 内容 | 状態 |
|---|---|---|
| U1 | **ブロック追加は基本作成者**が行う。Excel No.1–42 は追加候補／代表案件の見本。新規への強制初期42投入はしない | **CONFIRMED** |
| U2 | **システム入力工種**: 番号・名称のどちらか入力／DD選択で **相互反映**。手入力でも **候補表示**（候補源＝コード表） | **CONFIRMED** |
| U3 | **取引先**: 候補源＝Excel正 **`データマスタ!J`（見出し「取引先」）**。当面は **候補選択＋手入力可（コンボ）**（Excelでもリスト外入力あり。735は変更しない）。**将来**: リストを整備し **リストからの選択**に寄せる（手入力の要否は整備後に再確認） | **CONFIRMED** |
| U4 | **名称・規格**: 左2列は当面 **候補＋手入力可**、3列目は手入力。候補源＝Excel正 **`データマスタ!H`（種別）** ＋ブロックにより **`データマスタ!C` の部分範囲**。詳細は §8.5b。**将来**: リスト整備後に左2列も **リスト選択中心**へ（浜田 2026-07-20・案A＋将来方針） | **CONFIRMED** |
| U5 | 3列目は **全角カナを正**。半角カナ入力は入力確定時に **自動で全角カナ**へ変換 | **CONFIRMED** |
| U6 | **単位**は総括と同じリスト選択（`㎡`/`式`/`回`/`人`/`日`/`箇月`/`－`。足りなければ候補追加） | **CONFIRMED** |
| U7 | **単価未入力 → 金額も未入力（空白）**。数量×単価の自動計算は単価ありのときのみ | **CONFIRMED** |
| U8 | 明細行の **初期は1行** | **CONFIRMED** |
| U9 | 新規工事の内訳は **ブロック 0** から開始（作成者が最初のブロックを追加） | **CONFIRMED** |
| U10 | **名称・規格**は **3列フラット**。1列目は空可（必須にしない）。金額・数量はその行に持つ。あわせて内部に **名称規格グループ**（例: `現場経費`）を持ち、1列目空の行もグループで識別できる | **CONFIRMED** |
| U11 | **数量未入力 → 金額も未入力（空白）**。金額の自動計算は **数量ありかつ単価あり** のときのみ（U7と併用） | **CONFIRMED** |
| U12 | ブロック内明細は削除後も **最低1行**残す（給与手当と同型） | **CONFIRMED** |
| U13 | **名称規格グループ**の付け方: 1列目に値がある行がそのグループ名。続く1列目空の行は直前グループを **自動継承** | **CONFIRMED** |
| U14 | 工種ブロック **並び替え後**も内訳№を **表示順に詰め直す**（削除時と同型） | **CONFIRMED** |
| U22 | **内訳№の表示位置**: ブロック見出しで **システム入力工種の左**（自動・手入力なし）。Excel `BE` 右端はUI非踏襲 | **CONFIRMED** |
| U23 | **ブロック／明細の操作（案A）**: ブロック＝追加・削除・順番入替可。明細＝追加・削除・順番入替可。**フッタ行**（諸経費→各種保険料→小計→法定福利費→計）は **削除・並び替え不可**（明細帯の中だけ入替）。削除・入替後の内訳№は U14 | **CONFIRMED** |
| U24 | **1列目ブランク行の入替**: 明細入替のあと、ブロック内を上から再走査し **U13 の名称規格グループ継承を付け直す**（空1列目行は直上の「1列目あり」行のグループに付く）。見出し行削除で空行だけ残る場合も同様に再付着／グループ空 | **CONFIRMED**（浜田 2026-07-20・案A） |
| U15 | **同一合計の複数参照**: 合計は内部にブロックあたり1つ。総括・内訳のどこから見ても同じ金額。Excelの別人二重参照（例: No.3/No.4→同一 `AQ$88`）は **踏襲しない**。受入条件: **参照経路が違っても金額が変わらない** | **CONFIRMED** |
| U16 | 内訳の単位リスト: 総括共通（`㎡`/`式`/`回`/`人`/`日`/`箇月`/`－`）に加え **`缶` / `枚` / `％`**。足りなければ後から候補追加可 | **CONFIRMED** |
| U17 | 内訳明細の薄い赤: 請負と同型。名称（または名称・規格のいずれか）入力後に単位・数量・単価が空なら薄い赤。空の初期行は赤にしない。**フッタ固定行（諸経費〜計）は薄い赤の対象外**（U30） | **CONFIRMED** |
| U18 | **`#REF!` は移植しない**。明細の **金額**はとりあえず **数量×単価**（U7/U11: 両方ありのときのみ自動計算・整数表示）。壊れたExcel式の復元はしない | **CONFIRMED** |
| U19 | 単位が **`％`** の行の金額も同族: **単価 × ％**。％は **数量欄に入れた率**とし、計算は **単価×数量÷100**（整数表示）。通常単位の行は **数量×単価**のまま。**フッタ固定行**（諸経費・各種保険料・法定福利費）の金額は U20／C-U15・C-U16 の **手入力**が優先（専用％エンジンは作らない） | **CONFIRMED** |
| U20 | ブロック追加時の初期セット（この順）: **明細1行（空）→ 諸経費 → 各種保険料（任意保険）→ 小計 → 法定福利費 → 計**（**常にフルフッタ**。Excelの「計のみ」型は新規では用意しない）。使わないフッタ行の金額は **ブランクのまま**可。**諸経費・各種保険料・法定福利費の金額は手入力**（自動は依頼者確認後＝R-11/R-12/R-13）。**小計・計はシステム集計**（U25） | **CONFIRMED** |
| U21 | **セル種別の薄い色分け**（自動／手入力／リスト選択が視覚で分かる）: 背景を **ごく薄い色**で区別する（浜田 2026-07-20・案A）。**適用範囲はアプリ全体**: **総括・請負・内訳・予実管理**（および同画面の他ブロック）に同一3色ルール。未入力の薄い赤（U17／請負と同型）は **不足指摘が優先**。色値は下表（後で微調整可） | **CONFIRMED** |
| U25 | **小計・計の集計**（浜田 2026-07-20・Excel確認済）: **小計**＝明細の金額合計 ＋ 諸経費 ＋ 各種保険料。**計**＝小計 ＋ 法定福利費。空の手入力金額は合計に **0 扱いで足す（表示は空白のまま可）** | **CONFIRMED** |
| U26 | **コンボ欄のセル色**: 候補あり＋手入力可の欄は **常にリスト色（ごく薄い緑）**。手入力しても黄に切替えない（浜田 2026-07-20・案A）。未入力・区分警告等は **セル色を変えず**、赤字テキスト／警告メッセージで示す（U29） | **CONFIRMED** |
| U27 | **1列目ブランクの表示**: 継承グループ名は **内部のみ**。画面の1列目は **空のまま**（Excel寄り・案A）。むやみに空行を増やさないルールは U28 | **CONFIRMED** |
| U28 | **保存時クリーンアップ**（浜田 2026-07-20）: (1) 明細の **完全空行**（名称3列・数量・単価・備考がすべて空）は、ブロックあたり **最大1行まで残し**、2行目以降の完全空は **保存時削除**（入力用の空き1行を残す解釈）。(2) **中身のないブロック全体**も保存時に削除（定義は下）。削除後は内訳№を詰め直し（U14）。編集中の最低1行（U12）は維持 | **CONFIRMED** |
| U29 | **区分（施工／保安）**: ブロック見出しで **取引先の左**に配置。候補は **施工／保安**（リスト色）。総括への反映先はこの区分。未入力でも **一時保存・本保存は可能**だが、警告を赤字で表示: **`内訳№の区分が未入力です`**（№部分は当該内訳№。例: `No.3の区分が未入力です`）。複数あれば列挙または件数表示可。薄い赤セルと併用可（B4同型・保存阻害しない） | **CONFIRMED** |
| U30 | **フッタ行の薄い赤**: 諸経費・各種保険料・小計・法定福利費・計は **薄い赤の対象外**（金額ブランク可・ラベル固定。浜田 2026-07-20・案A） | **CONFIRMED** |

| 種別 | 対象の例 | 背景（ごく薄い・CONFIRMED） |
|---|---|---|
| **自動** | 金額（数量×単価／％計算）、小計・計、日数、相互反映で埋まった側、内訳№ 等 | ごく薄い青 `#F3F8FC` |
| **手入力** | フリー文字・数量・単価・備考・諸経費／保険料／法定福利の金額・契約工種（総括）等 | ごく薄い黄 `#FFFCF3` |
| **リスト選択** | 単位DD、**区分（施工／保安）**、取引先・名称規格（左2列）のコンボ欄（手入力しても緑のまま＝U26）、候補付き工種番号／名称 等 | ごく薄い緑 `#F4FAF4` |

**U28 補足 — 中身のないブロック（保存時削除）**:
- システム入力工種（番号・名称）が空、かつ
- 取引先が空、かつ
- **区分が空**、かつ
- 明細がすべて完全空（または実質データなし）、かつ
- 諸経費・各種保険料・法定福利費の金額がすべて空
→ そのブロックを保存時に削除。小計・計だけの自動0は「中身」とみなさない。

**ブロック見出しの並び（CONFIRMED・抜粋）**:
`内訳№` → `システム入力工種` → … → **`区分`（施工／保安）** → **`取引先`** → …

**D-50〜D-61**: U1〜U19。**D-62**: U20。**D-63**: U21。**D-65**: U22。**D-66–73**: U23–U30。**D-74**: 内訳 CLOSED。  
次ラウンド: **予実管理**（夜）。

### 8.5c AIチーム横断レビュー（2026-07-20）

参加者視点: CIO（本機）／DeepSeek／Sequential／OpenRouter。仕様のみ・実装なし。

**偽陽性として棄却**（仕様上すでに整理済）:
- 「フッタ固定」と「ブロック削除」の衝突 → フッタ固定はブロック**内**操作。ブロック削除時はフッタごと削除。
- 1列目空と金額計算の矛盾 → 金額は数量×単価（または％）のみ。1列目はグループ用。
- ％式と通常式の並存 → U19で分岐済。
- 薄い赤 vs 3色 → 不足指摘レイヤが優先（U21）。
- 「H+C」＝履歴+コードという誤解 → **データマスタ列 H / C**（§8.5b）。表記注意。

**残る論点（優先）**:
1. ~~**小計・計の集計範囲**~~ → **U25 / C-U22 CONFIRMED**
2. ~~新規ブロックは常にフルフッタか、Excel材料型の「計のみ」も許すか~~ → **U20: 常にフルフッタ・不要行は金額ブランク**
3. ~~コンボ（候補＋手入力）欄のセル色は緑か黄か~~ → **U26: 常にリスト緑**
4. ~~U24再計算後、1列目は見た目も空のままか~~ → **U27: 見た目は空・グループは内部のみ**
5. ~~空行／空ブロック~~ → **U28: 保存時クリーンアップ**
6. ~~施工／保安区分~~ → **U29: 取引先の左。未入力は赤字警告のみ（保存可）**
7. ~~フッタ固定行は薄い赤の対象外か~~ → **U30: 対象外**

**誤解しやすい表記の修正方針**: 「H+C」は必ず「データマスタ!H / !C」と書く。

**AI横断レビュー論点は上記まで消化。**

### 8.6 内訳ラウンド CLOSED — 最終合意（2026-07-20 昼）

| 項目 | 内容 |
|---|---|
| 判定 | **内訳ラウンド CLOSED** |
| 浜田 | 一問ずつ確認済（U1–U30 / C-U01–C-U25）。最終区切り指示 2026-07-20 |
| AI合意 | CIO＋DeepSeek＋Sequential＋OpenRouter — **合意可**（残リスクは依頼者確認・実装GO前に限定） |
| 残置（依頼者／後続） | **R-11** 諸経費正式式 / **R-12** 法定福利係数 / **R-13** 各種保険料正式式 / 将来の取引先・名称規格リスト整備 |
| 非対象のまま | 実装・deploy・App作成・現行736改修 — **浜田 GO 後** |
| 次ラウンド | **予実管理**（2026-07-20 夜予定）→ 版管理。**予実完了後**: 依頼者確認メール用リスト作成（R-11/12/13 ほか） |

**夜セッションへの引き継ぎ（1文）**: 内訳確定を前提に予実（§9）を一問ずつ確定し、その後依頼者へ送る確認事項リスト（メール用）を起こす。

**D-74**: 内訳ラウンド CLOSED（2026-07-20）。

**確認事項リスト追記**:
| ID | 確認事項 | 決定 |
|---|---|---|
| C-U15 | 諸経費・各種保険料の金額 | **手入力**（式不明。自動式は依頼者確認後） |
| C-U16 | 法定福利費の金額 | **手入力**（式の業務正が不明なら手入力。自動は依頼者確認後＝R-12） |
| C-U17 | セル背景の種別色 | **案A＋全体適用**: 自動／手入力／リストの3色を **総括・請負・内訳・予実**に共通。色はごく薄い青／黄／緑。薄い赤は不足指摘優先 |
| C-U18 | 取引先候補源 | **案A**: Excel正 `データマスタ!J`。当面は候補＋手入力可。**将来はリスト整備→選択中心** |
| C-U19 | 名称・規格候補源 | **案A**: Excel正 `データマスタ!H`＋`C`部分。当面は候補＋手入力可（3列目は手入力）。**将来はリスト整備→左2列は選択中心** |

**U15 観測メモ（Excel）**: `総括!AH40` と `AH41`（No.3/No.4）がともに `内訳!$AQ$88`；`AH72`/`AH73`（No.33/No.34）がともに `$AQ$618`。壊れたリンク疑い。

**U10 補足（CONFIRMED）**:
- UI・入力は Excel に近いフラット3列（案A）。
- 内部キー例: `name_spec_group`＝`現場経費`（親見出し相当）。配下行は U13 で継承。
- グループの付け方は **U13**。

### 8.5a 内訳ラウンド確認事項リスト（浜田確認済・一覧用）

実装・検収・説明で参照する短い一覧。詳細は上表 U* / D-*。

| ID | 確認事項 | 決定 |
|---|---|---|
| C-U01 | ブロック追加の主体 | 作成者（新規はブロック0。No.1–42は候補／見本） |
| C-U02 | システム入力工種 | 番号⇔名称の相互反映＋候補（コード表） |
| C-U03 | 取引先 | **データマスタ!J**・候補＋手入力可 |
| C-U04 | 名称・規格 | 左2列＝**H（種別）＋C部分**の候補＋手入力可／3列目手入力・全角カナ正 |
| C-U05 | 現場経費型（1列目空） | 3列フラット＋内部グループ自動継承 |
| C-U06 | 金額（通常） | **数量 × 単価**（片方空なら金額空） |
| C-U07 | 金額（単位が％） | **単価 × ％**（％＝数量欄の率。式は単価×数量÷100）※本項として残置 |
| C-U08 | 単位リスト | 総括共通＋`缶`/`枚`/`％` |
| C-U09 | 同一合計参照 | ブロックあたり1ソース。参照経路で金額不変。Excel二重参照は非踏襲 |
| C-U10 | `#REF!` | 移植しない。金額は C-U06/C-U07 |
| C-U11 | 薄い赤 | 請負と同型（明細のみ）。**フッタ行は対象外** |
| C-U12 | 明細行数 | 初期1行・削除後も最低1行 |
| C-U13 | 内訳№ | 削除・並び替え後とも詰め直し。表示はシステム入力工種の左（自動） |
| C-U14 | ブロック追加時の初期行セット | **常にフルフッタ**（明細1→諸経費→保険料→小計→法定福利→計）。不要行の金額はブランク可 |
| C-U20 | ブロック／明細操作 | **案A**: ブロック追加削除入替可。明細追加削除入替可。フッタは固定 |
| C-U21 | 1列目ブランク入替 | **案A**: 入替後に U13 継承を再計算 |
| C-U23 | 保存時クリーンアップ | 完全空の明細は最大1行／余剰削除。中身なしブロックも保存時削除。№詰め直し |
| C-U24 | 区分（施工／保安） | 取引先の左。未入力時は一時保存・保存で赤字警告のみ（保存可） |
| C-U25 | フッタの薄い赤 | **対象外**（諸経費〜計） |
| C-U15 | 諸経費・各種保険料の金額 | **手入力**（式不明。自動は依頼者確認後） |
| C-U16 | 法定福利費の金額 | **手入力**（業務正未確認。自動は依頼者確認後＝R-12） |
| C-U22 | 小計・計 | **小計**＝明細合計＋諸経費＋各種保険料。**計**＝小計＋法定福利費 |
| C-U17 | セル背景の種別色 | **案A＋全体**: 自動＝ごく薄い青 `#F3F8FC`／手入力＝ごく薄い黄 `#FFFCF3`／リスト＝ごく薄い緑 `#F4FAF4`。**総括・請負・内訳・予実**に共通。薄い赤＝不足指摘が優先。**コンボ欄は常に緑**（U26） |
| C-U18 | 取引先候補源 | **案A**: `データマスタ!J`。当面＝候補＋手入力可。将来＝リスト整備後に選択中心 |
| C-U19 | 名称・規格候補源 | **案A**: `データマスタ!H`＋`C`部分。当面＝候補＋手入力可（3列目手入力）。将来＝リスト整備後に左2列は選択中心 |

### 8.5b Excelリスト参照先（2026-07-20 実測）

詳細ログ: `docs/plans/2026-07-20-jikkou-list-source-scan.md`

| 用途 | Excel参照 | 備考 |
|---|---|---|
| 取引先 | `データマスタ!$J$3:$J$103`（一部 `$J$3:$J$15` / `$J$3:$J$102`） | 見出し「取引先」。内訳 `U3` 付近（Y/AA帯）の DD |
| 名称・規格（種別・グループ） | `データマスタ!$H$3:$H$103` ほか `$H$3:$H$11` / `$H$4` / `$H$6` | 見出し「種別」（材料費・労務費・現場経費・諸経費等） |
| 名称・規格（カテゴリ／製品） | `データマスタ!$C$72:$C$80`（塗料・鋼材費…）、`$C$82:$C$100`（塗料製品名など） | ブロックにより参照範囲が異なる |
| 現場経費固定帯 | 直書き `"現場経費"` | 特定行帯のみ |
| 明細（I列） | `データマスタ!$I$2:$I$103` | 見出し「明細」。取引先帯と **同一セルに二重DV** あり。試作の取引先正は **J列** |

**リスト外（手入力）の実測例**:
- 名称側: `労務費（夜間）`（Hリスト外×14）、`工具･機械使用料`（短いHリスト外×13）、`塗装記録表示シール`（C82–100外×2）
- 取引先帯: `今岡塗装` 等（I照合では外・運用上の手入力）

→ **当面は候補必須にしない**（コンボ・手入力可）。コンボ欄の色は **常にリスト緑**（U26）。手入力のみ欄は黄。
→ **将来方針（浜田 2026-07-20）**: リストを準備し、取引先・名称規格（左2列）は **リストから選択**できるようにする。整備後に手入力の残置要否を再確認。

**Excel観測（ブロックフッタ・2026-07-20 実測）**:
- **計**: 全 **42** ブロックに存在。
- **諸経費 / 小計 / 法定福利費**: 実測では **14** ブロック（No.3型）。材料中心の No.1 等は **計のみ**の型もあり。試作の初期セット（浜田）: **明細1（空）→諸経費→各種保険料（任意保険）→小計→法定福利費→計**。
- No.3/No.4 の健全寄りの式（参考・試作ではフッタ金額は手入力）:
  - 明細金額: `ROUND(数量*単価,0)`
  - 諸経費（単位％・読めるとき）: `ROUND(数量/100*単価,0)`（単価セル＝直上明細SUM）→ **正式式は R-11**
  - 小計: `SUM(明細先頭〜諸経費/保険料)` → **U25 CONFIRMED**（明細合計＋諸経費＋各種保険料）
  - 法定福利費: `ROUND(基数*0.9*0.1515,0)` → **R-12**（試作は手入力）
  - 計: `SUM(小計:法定福利費)` → **U25 CONFIRMED**（小計＋法定福利費）

## 9. 予実管理

### 9.1 初回試作の入力方式 — CONFIRMED（浜田合意 2026-07-19）

- 実績は利用者が **消費した税抜金額を手入力**する（整数円・§15.3）。
- 予算は内訳から取得する（給与手当は総括行から取得）。
- **残予算 = 予算 − 実績** を表示する。
- **消化率 = 実績 ÷ 予算** を表示する。
- 工種別、および **施工 / 保安 / 給与手当** の3区分別に予算・実績・残予算・消化率を集計する。
- 予算0の場合の消化率は **「—」表示（計算しない）** — **CONFIRMED**（浜田 2026-07-20・§15.7）。0除算を数値0にしない。
- **セル種別色（浜田 2026-07-20・U21）**: 予実管理も同一3色（実績＝手入力色、予算・残・消化率＝自動色 等）。詳細は §8.5 U21。

### 9.2 実績の帰属 — CONFIRMED

実績は予算版ではなく **工事に帰属**する。
予算版を作るたびに実績を複製してはならない。
予算確定後も、権限の範囲内で実績は別データとして編集可能とする。

### 9.3 版との監査関係 — CONFIRMED

- 実績登録時点で基準となっていた予算版を保存または参照できるようにする。
- 監査時に「この実績がどの予算版の時点で入力されたか」を追跡できること。
- 表示時は最新確定版、または利用者が選んだ過去版との対比を可能にする。
- 対比版を変えても実績そのものを複製・書換えしない。
- 実績は表示名ではなく、版をまたいで不変の **工種キー** と **費用区分キー** に帰属させる。
- 比較版で工種キーが廃止・分割・統合された場合の表示・再配賦は **OPEN** とし、初回試作では対象外とする。
- 過去版選択時の初期値、選択保持、表示警告は **OPEN**。

### 9.4 実績フィールド

| 項目 | 要件 | 状態 |
|---|---|---|
| 実績日 | 消費・計上の日付 | **CONFIRMED** |
| 工種キー | 内訳・集計と対応する不変キー | **CONFIRMED** |
| 費用区分キー | `施工` / `保安` / `給与手当` を識別する不変キー | **CONFIRMED**（3区分・§15.4） |
| 実績金額 | 税抜金額 | **CONFIRMED** |
| 内容 | 摘要・説明 | **CONFIRMED** |
| 取込元種別 | 初回は `手入力` | **CONFIRMED** |
| 登録時予算版 | 実績登録時点の予算版識別子 | **CONFIRMED** |
| 取込元レコード参照 | 将来連携元を追跡する参照 | **OPEN / 初回試作対象外** |

作成者、作成日時、更新者、更新日時、訂正理由、削除状態等の監査項目は **PROVISIONAL**。
初回試作で保証する監査範囲は **登録時予算版の保持** までとする。
実績の訂正履歴、訂正理由、論理削除は **OPEN** であり、確定前は完全な変更監査を受入条件にしない。

### 9.5 将来構想 — OPEN / 今回スコープ外

将来は App 678 に似た運用や、発注・支払データとの連携を検討し得る。
連携元、突合キー、計上時点、取消・返品、税処理、権限は未解決であり、初回試作には含めない。

## 10. バージョン管理・差分

### 10.1 基本要件 — CONFIRMED

- 1工事に複数の予算版を持つ。
- 修正時は直前の確定版を全量複製して新しい下書きを作る。
- 1工事につき下書きは1件だけとする。
- 内訳明細は安定した行キーを持ち、版複製時に引き継ぐ。
- 直前版、当初版、または利用者が選択した版と比較できる。
- 差分を総括、工種、明細の各レベルで確認できる。
- 追加、削除、値変更、および総括への影響を区別する。

### 10.2 確定時即時不変 — CONFIRMED / 旧仕様を明示上書き

**予算版を確定した時点で、その予算金額・明細・内容は直ちに変更不可とする。**
誤りの訂正も、確定版を直接編集せず、新しい版を作成して行う。

このルールは、既存版管理仕様の R-12
「版確定後も、次版を作るまでは編集可」を Ver.02 について明示的に上書きする。
本ドラフトでは管理者解除、緊急 unlock、確定取消は設けない。

実績は予算版とは別管理のため、予算版確定後も実績訂正ルールに従って編集可能とする。

### 10.3 状態遷移 — CONFIRMED

| 状態 | 予算編集 | 次版作成 | 備考 |
|---|---|---|---|
| 下書き | 可 | 不可 | 1工事1件 |
| 版確定 | 不可 | 可 | 確定操作直後から不変 |

確定操作の実行者、権限、確認ダイアログ、二重実行防止は **OPEN**。

## 11. データモデル方針

### 11.1 論理エンティティ — PROVISIONAL

| エンティティ | 帰属・役割 |
|---|---|
| 工事 | 基本情報と全版・全実績を束ねる |
| 予算版 | 工事配下の下書きまたは確定スナップショット |
| 内訳明細 | 予算版配下。安定行キーを版間で継承 |
| 実績 | 工事配下。登録時予算版を監査参照 |
| マスタ | 総括・内訳の入力候補、コード、分類を供給 |

アプリ分割、サブテーブル使用、ルックアップ方式、レコードID構成は **OPEN**。

### 11.2 必須の追跡関係 — CONFIRMED

1. 工事から全予算版を追える。
2. 予算版から複製元版を追える。
3. 内訳行を版間で同一行として追える。
4. 実績から工事、工種、施工/保安区分、登録時予算版を追える。
5. 表示中の比較対象版を明示できる。

## 12. マスタ登録方針

### 12.1 分類ルール — CONFIRMED

| 状態 | 扱い |
|---|---|
| 総括/内訳で参照され、意味も理解できる | 正式マスタ候補として登録対象 |
| 参照されているが意味・用途が不確か | **PROVISIONAL / 要確認** として保持 |
| 確認できる参照がない | 正式登録せず「未参照候補」へ |

元データは失わず保存する。
`#REF!` が参照を隠している可能性があるため、未参照候補を「未使用」と断定しない。
実装 GO と依頼者確認の双方が揃うまでは、候補一覧を読取レビュー資料として保持し、
App 735 その他の kintone アプリへ登録・更新・削除しない。

### 12.2 未参照候補・依頼者レビュー一覧

一覧には最低限、次の列を持たせる。

| 列 | 内容 |
|---|---|
| シート/セル | 出典位置 |
| コード/名称 | 元データの識別 |
| 推定用途 | **PROVISIONAL** な用途 |
| 観測された参照 | 読める式・ラベル・配置等 |
| 重複/廃止可能性 | 同義語、旧値、重複の可能性 |
| 質問 | 依頼者に確認する一点 |
| 処置 | 正式登録 / 要確認保持 / 未参照候補継続 / その他 |

## 13. 試作対象外

次は 2026-07-25 試作の対象外とする。

- 印刷。アプリが最終形に近づくまで着手しない。
- Excel ファイル出力。
- 承認ワークフロー。
- App 678 との完全連携。
- 発注・支払データの自動取込。
- 本番移行、既存データ移行、Ver.01 統合。
- 視覚デザインの最終仕上げ。

## 14. 代表シナリオ

### 14.1 依頼者レビュー用 — CONFIRMED

具体金額は依頼者と設定し、ここでは創作しない。

1. 代表工事を1件作成し、基本情報を入力する。
2. 内訳に複数の工種ブロックを置き、施工費と保安費の明細を入力する。
3. 内訳から総括の①⑧⑨候補と、工種別予算集計への流れを確認する。
4. 予算版1を確定し、直後に編集不可になることを確認する。
5. 工事に対して税抜実績を手入力し、登録時予算版が版1で記録されることを確認する。
6. 工種別・施工/保安別の実績、残予算、消化率を確認する。
7. 版1から版2下書きを作り、内訳を変更して確定する。
8. 版1対版2の総括・工種・明細差分を確認する。
9. 同じ実績を複製せず、版2および選択した版1の双方に対して表示比較する。

## 15. 2026-07-20 確認事項（1問ずつ）

### 15.0 進め方 — CONFIRMED（浜田 2026-07-20）

1. **基本は受領 Excel を正**とする（レイアウト・見出し・保存表示値・読める数式・区分構造）。
2. **こちらでできることは先に仕様化・試作設計まで進める**（確認待ちで止めない）。
3. **できないところだけ**依頼者確認リストへ回す。
4. **予想で埋めた箇所**は必ず「予想で作成」リストに載せ、CONFIRMED と混同しない。
5. `#REF!`・意味不明は推測で断定せず、予想リストまたは依頼者確認リストへ。
6. 依頼者回答で予想→CONFIRMED に昇格する。
7. 優先ソース: **読める Excel ＞ 浜田確定 ＞ 予想（明示ラベル） ＞ 依頼者確認（Excel不能時）**。
8. Ver.01 は参考。Excel と衝突時は読める Excel 優先。

| # | 論点 | 状態（2026-07-20） |
|---|---|---|
| 1 | **計算権威 / 進め方** | **CONFIRMED**（上記 15.0） |
| 2 | **①⑧⑨** | **CONFIRMED**（浜田 2026-07-20・§15.2） |
| 3 | **税基準と丸め** | **CONFIRMED**（税抜・金額は積のままROUNDなし・整数表示／§15.3・X2） |
| 4 | **実績訂正権限** | OPEN（依頼者確認） |
| 5 | **実績締め** | OPEN（依頼者確認） |
| 6 | **工種ブロック** | **CONFIRMED**（§15.6・追加→総括反映）／境界細部は OPEN |
| 7 | **原価3区分（施工・保安・給与手当）** | **CONFIRMED**（浜田 2026-07-20・§15.4）／明細帰属は OPEN |
| 8 | **マスタ不明項目** | OPEN（依頼者確認リスト化） |
| 9 | **予算確定者** | OPEN（依頼者確認） |
| 10 | **選択過去版表示** | OPEN（依頼者確認） |
| 11 | **予算0の消化率** | **CONFIRMED**（「—」・§15.7） |
| 12 | **App 678 将来連携** | OPEN / 初回試作外 |
| 13 | **試作場所** | **CONFIRMED**（Space 56・アプリ名確定／§15.8） |
| 14 | **版メタ情報** | OPEN（依頼者確認） |
| 15 | **代表案件** | **CONFIRMED（仮置き）**（§15.9）／期待値は依頼者確認 |
| 16 | **工種キー変更** | OPEN / 初回試作外寄り |
| 17 | **実績監査** | OPEN（依頼者確認） |

### 15.1 ワークブックから先に確定できる観測（2026-07-20）

対象ファイル: `C:\tmp\実行予算ver2\工事予算（実行予算）(案) (1).xlsx`  
（ロックファイル `~$…` は読取対象外）

| 観測 | 根拠セル／事実 | 状態 |
|---|---|---|
| シート構成は4枚 | `コード表` / `総括` / `内訳` / `データマスタ` | **CONFIRMED**（再確認） |
| 数式に `#REF!` 多数 | `#REF!` 式 **563** / その他式 **1027** | **CONFIRMED**（再確認） |
| 総括に①ラベル | `総括!AJ34` = `…①`（近傍 `AE34` が合計式） | **CONFIRMED（ラベル観測）** |
| 総括に⑧ラベル | `総括!AR90` = `上記項目の合計値…⑧`（`B90`=`工事原価額及び率`） | **CONFIRMED（ラベル観測）** |
| 総括に⑨ラベル | `総括!AR91` = `①-⑧…⑨`（`A91`=`粗利額及び率`） | **CONFIRMED（ラベル観測）** |
| 総括の原価は施工→保安→給与手当→⑧ | `B38`施工 / `B76`保安 / `B84`給与手当 / `AM90=SUM(AM75,AM83,AM89)` | **CONFIRMED**（§15.4） |
| 金額列に税抜表示 | `AE9`/`AM37`/`BF36` 等に「金額（税抜）」「工事予算消化金額（税抜）」 | **CONFIRMED** → §15.3（税抜・整数円） |
| コード表に施工費・保安費 | `コード表!B4`=`施工費`、`B60`=`保安費` | **CONFIRMED（見出し観測）** |
| 内訳→総括の単価参照に壊れ | 例: `総括!AH38`→`内訳!$AQ$30` が `#REF!` | **CONFIRMED（読取事実）**／正しい参照先は **依頼者確認** |

### 15.2 ①⑧⑨ — CONFIRMED（浜田 2026-07-20）

| 記号 | 意味 | 状態 |
|---|---|---|
| **①** | 売上側合計 | **CONFIRMED** |
| **⑧** | 工事原価合計 | **CONFIRMED** |
| **⑨** | 粗利（**①−⑧**） | **CONFIRMED** |

- 総括ラベル（`…①` / `上記項目の合計値…⑧` / `①-⑧…⑨`）と一致。
- **依頼者確認に残す**: ①⑧⑨それぞれの内訳構成要素の正式名称・並び・消費税行の扱い。

### 15.3 税基準と丸め — CONFIRMED（浜田 2026-07-20）

| 項目 | 決定 | 状態 |
|---|---|---|
| 金額の正 | **税抜**（請負）。原価は税抜が⑧の正・税込は併記 | **CONFIRMED** |
| 金額の単位 | **整数円**（小数なし） | **CONFIRMED**（浜田） |
| 端数処理の具体 | **Excel正**: 金額式は `数量×単価`（**ROUNDなし**）。数量は小数可（表示例 小数1位）。金額は計算結果のまま・**表示は小数なし（`#,##0`）**。基幹との1円差は作成者が数量・単価で調整（X1）。B2注記の四捨五入は基幹／運用側 | **CONFIRMED**（浜田＋Excel観測 2026-07-20・X2） |

1. ~~**計算権威**~~ → **§15.0 で CONFIRMED**
2. ~~**①⑧⑨**~~ → **§15.2 で CONFIRMED**（構成要素の詳細は依頼者確認）
### 15.4 原価3区分 — CONFIRMED（浜田 2026-07-20・Excel正）

**⑧（工事原価合計）＝ 施工計 ＋ 保安計 ＋ 給与手当計**（浜田承認済み）

| 区分 | 根拠 |
|---|---|
| 施工 | `B38` 見出し、小計 `AM75` |
| 保安 | `B76` 見出し、小計 `AM83` |
| 給与手当 | `B84` 見出し、小計 `AM89`（現場代理人・監理技術者・工事担当者等） |
| ⑧合算 | `AM90 = SUM(AM75,AM83,AM89)`、ラベル `上記項目の合計値…⑧` |

**依頼者確認に残す**: 各明細行の区分帰属ルール、給与手当の工種キーとの関係、コード表との対応。

3. ~~**税基準と丸め**~~ → **§15.3 で CONFIRMED**（端数処理の具体は依頼者確認）
4. **実績訂正権限**: 誰が実績を作成・修正・削除でき、訂正理由や履歴を必須にするか。
5. **実績締め**: 月次・工事完了等の締切後に訂正を許すか。
### 15.6 工種ブロック — CONFIRMED（浜田 2026-07-20）

1. 内訳は Excel どおり **工種ブロック**で持つ。
2. **ブロックを追加できる**（現行 736 詳細表と同趣旨）。
3. **追加したブロックは総括へ追加・反映できる**（予算供給・原価行連携）。
4. 列・境界の細部は Excel を正に抽出。読めない所は依頼者確認。

6. ~~**工種ブロック**~~ → **§15.6 / §8.2 CONFIRMED**
7. ~~**施工/保安区分**~~ → **§15.4 CONFIRMED**（⑧＝施工＋保安＋給与手当。明細帰属は依頼者確認）
8. **マスタ不明項目**: `コード表` と `データマスタ` の要確認・未参照候補をどう処置するか。
9. **予算確定者**: 誰が版を確定でき、確認前警告や権限制御をどうするか。
10. **選択過去版表示**: 既定は最新確定版か。過去版選択を画面・利用者・期間ごとに保持するか。
### 15.7 予算0の消化率 — CONFIRMED（浜田 2026-07-20）

| 条件 | 表示 |
|---|---|
| 予算 = 0（実績の有無を問わない） | **「—」**（消化率は計算しない） |
| 予算 > 0 | `実績 ÷ 予算`（％桁は当面小数第1位仮置き・A-01。予実ラウンドで再確認） |

11. ~~**予算0の消化率**~~ → **§15.7 CONFIRMED**
12. **App 678 将来連携**: 将来必要な発注・支払・実績の粒度と参照キーは何か。
### 15.8 試作場所 — CONFIRMED（浜田 2026-07-20）

| 項目 | 決定 |
|---|---|
| スペース | **[Space 56](https://jbis-kintone.cybozu.com/k/#/space/56)**（Ver.01 736 と同スペース） |
| アプリ名 | **実行予算書作成支援ツールver02** |
| App ID | 未採番（**実装 GO 後**に新規作成時に確定） |
| 現行736 | **触らない**（rev186 維持） |
| マスタ735 | **変更しない**（試作が参照する場合も読取のみ） |

**本書のこの決定はアプリ作成・deploy の許可ではない。** 実装 GO 後に作成する。

13. ~~**試作場所**~~ → **§15.8 CONFIRMED**
14. **版メタ情報**: 版種別、変更理由、立案日、確定者、確定日時の必須範囲は何か。
### 15.9 代表案件 — CONFIRMED（仮置き・浜田 2026-07-20）

| 項目 | 内容 |
|---|---|
| 出典 | 受領 Excel `総括` |
| 工事コード | `2423101` |
| 工事名称 | 根岸貨物線　田立線路橋 |
| 発注者 | JR東日本横浜支社 |
| 正式名称 | ２０２４年度横浜支社管内橋りょうペイント塗替その他工事 |
| 期待出力値 | **創作しない**。試作検算時は Excel 保存表示値を正とし、依頼者確認で最終化 |

15. ~~**代表案件**~~ → **§15.9 CONFIRMED（仮置き）**
16. **工種キー変更**: 版間で工種を廃止・分割・統合した場合、既存実績をどう表示・再配賦するか。
17. **実績監査**: 訂正理由、変更履歴、論理削除のどこまでを初回実装に含めるか。

### 15.5 進捗メモ（2026-07-20 時点）

| 確定済 | 内容 |
|---|---|
| §15.0 | Excelを正／読める範囲で先行／不明は依頼者確認 |
| §15.2 | ①売上合計・⑧工事原価・⑨粗利（①−⑧） |
| §15.3 | 税抜・整数円（端数処理の具体は依頼者） |
| §15.4 | ⑧＝施工＋保安＋給与手当 |
| §15.6 | 工種ブロック追加可／総括へ反映（736詳細表同趣旨） |
| §8.3 | 内訳№連番／給与手当は総括のみ行追加 |
| §15.7 | 予算0の消化率は「—」 |
| §15.8 | Space 56・アプリ名「実行予算書作成支援ツールver02」（未作成） |
| §15.9 | 代表案件仮置き 2423101／田立線路橋（期待値は依頼者） |

**§16 必須項（2,3,6,7,11,13,15）はすべて浜田側 CONFIRMED。**  
**追加 CONFIRMED**: ブロック削除→総括連動削除／削除後の内訳№詰め直し（暫定）。  
**作業リスト**: §20（できること D-*／予想 A-*／依頼者 R-*）。  
**ブロック台帳**: [`2026-07-20-jikkou-yosan-ver02-block-inventory.json`](2026-07-20-jikkou-yosan-ver02-block-inventory.json)（No.1–42＋給与5行）。

## 16. 2026-07-25 試作の受入条件

§15 のうち少なくとも **2、3、6、7、11、13、15** が確定し、
浜田の明示的な実装 GO を得た場合に限り、次をすべて満たすものを
「依頼者へ見せられる動作試作」とする。

**2026-07-20 時点**: 上記7項は浜田確認により **CONFIRMED**（§15.2–15.4, 15.6–15.9）。  
**実装 GO は未付与。** アプリ作成・deploy は行わない。
代表案件の入力値と期待出力は、依頼者資料または浜田確認値から作成し、創作しない。

- [ ] 4タブが存在し、役割が混在していない。
- [ ] 代表工事で基本情報から内訳、総括へ追跡できる。
- [ ] 内訳を工種ブロックで確認できる。
- [ ] 内訳で工種ブロックを追加でき、総括へ反映できる。
- [ ] 施工・保安・給与手当の3区分を区別して集計できる。
- [ ] 内訳予算が予実管理へ一意に供給される。
- [ ] 税抜実績を手入力できる。
- [ ] 実績が予算版ではなく工事に1回だけ所属する。
- [ ] 実績登録時予算版を監査参照できる。
- [ ] 実績、残予算、消化率を工種別・施工/保安/給与手当別に表示できる。
- [ ] 最新確定版と選択過去版の双方に対して同じ実績を比較表示できる。
- [ ] 1工事1下書きと、確定版からの次版複製を確認できる。
- [ ] 予算版は確定操作直後から編集不可になる。
- [ ] 訂正は新しい予算版で行える。
- [ ] 行キーにより、総括・工種・明細の差分を説明できる。
- [ ] `#REF!` 由来の推測が **PROVISIONAL** として識別されている。
- [ ] マスタの要確認項目と未参照候補一覧を依頼者へ提示できる。
- [ ] 計算・税・丸めの確定事項と未決事項を混同していない。
- [ ] App 736 Ver.01 rev186 と App 735 に変更がない。
- [ ] 試作が本番 deploy 済みであることを受入条件にしない。
- [ ] 印刷、Excel 出力、承認WF、App 678 完全連携を含めていない。

## 17. 日程と GO ゲート

| 日付 | 予定 | 条件 |
|---|---|---|
| 2026-07-20 | ワークブック分析と詳細仕様決定 | §15を1問ずつ確認 |
| 2026-07-21 | 実装開始可能日 | コア計算・データモデル確定、かつ浜田の明示的な実装 GO |
| 2026-07-25 | 依頼者へ見せられる成果物の目標 | 実装 GO 取得時は §16 の動作試作。GO未取得時は画面モック、データ対応表、試験シナリオ、未決事項一覧からなる非実装レビュー資料 |

**本書による実装 GO は付与されていない。**
2026-07-21 に自動着手せず、浜田の明示指示を待つ。
実装 GO 未取得のまま動作試作の完成を約束しない。

## 18. 既存仕様との矛盾・上書き事項

| 論点 | 既存記載 | Ver.02 の扱い |
|---|---|---|
| 確定版ロック | R-12 は次版作成まで編集可 | **確定直後に不変**。本書が明示上書き |
| 画面タブ | 総括表/詳細表、後に版管理追加 | **総括表/内訳/予実管理/バージョン管理** の4タブ |
| 詳細構造 | 材料②③・外注④〜⑦中心 | 新ワークブックの **工種ブロック内訳** を正として再確認 |
| 計算正本 | 旧 `書式.xls` と既存サンプル | 新ワークブックは563件の `#REF!` があり、完全復元を主張しない |
| 印刷 | Ver.01 では実装済み | Ver.02 試作では除外 |
| 本番識別 | 既存文書に rev185 等 | 現在信頼できる識別は rev186。deploy 前に記録衝突を解消 |
| 予実 | 旧 Ver.01 では非ゴール | Ver.02 の主要タブ。ただし初回は税抜実績の手入力 |

## 19. ドラフト完了条件

本ドラフトは次を満たす。

- 確定事項、仮説、未決事項を分離した。
- 新ワークブックの破損参照制約を明記した。
- Ver.01 保護と deploy 禁止を明記した。
- 予算版と実績の帰属を分離した。
- 確定時即時不変の新ルールを明記した。
- 明日の質問、代表シナリオ、試作受入条件、日程を定義した。

2026-07-20: §15 必須項と進め方（できることは先に進める／予想はリスト化）を反映済み。

## 20. 作業リスト（2026-07-20）— できること／予想／依頼者確認

**運用（浜田）**: 基本はこちらで作成できるところまで進める。できないところだけ確認する。予想で埋めた部分は下表「予想で作成」に必ず載せる。

### 20.1 こちらで作成できる／作成済み（Excel正＋浜田確定）

| ID | 内容 | 根拠 |
|---|---|---|
| D-01 | 4タブ（総括／内訳／予実／バージョン） | §6.1 CONFIRMED |
| D-02 | ①＝売上合計、⑧＝工事原価、⑨＝粗利（①−⑧） | §15.2 |
| D-14 | 請負金額＝①の明細。①＝施工計(AE19)＋保安計(AE33) | §7.1a・`SUM(AE19,AE33)` |
| D-15 | 請負金額は施工／保安とも行追加可。初期は各1行 | §7.1a・浜田 |
| D-16 | 請負金額は削除可。施工／保安とも最低1行は残す | §7.1a・浜田 |
| D-17 | 請負金額の数量は小数第1位まで | §7.1a・浜田＋Excel `1639.6` 等 |
| D-18 | （拡張→D-29）単位DD | §7.1a |
| D-19 | 契約工種入力後、単位・数量・単価の未入力を薄い赤等で明示 | §7.1a・浜田 |
| D-20 | 請負・備考はフリー入力（任意） | §7.1a・浜田 |
| D-21 | 請負金額＝数量×単価の自動計算。契約工種なし行は金額空白 | §7.1a・浜田・`AE=V*Z` |
| D-22 | 契約工種は当面手入力。DD化は将来別相談 | §7.1a・浜田 |
| D-23 | 請負は金額（税抜）のみ。税込・消費税列なし | §7.1a・浜田（税込併記案は撤回） |
| D-24 | 請負＝備考1列。原価＝計算基準／備考の2列分割 | §7.1a・浜田 |
| D-25 | 原価は消費税列＋金額（税抜）＋金額（税込）。⑧は税抜 | §7.1a・浜田 |
| D-26 | 消費税率DD表示 0％/8％/10％、内部 0/0.08/0.1。新規初期10％ | §7.1a・浜田 |
| D-27 | 施工・保安原価行の増減は内訳連動のみ。総括単独の行追加削除はしない | §8.2・浜田 |
| D-28 | 工種番号・システム入力工種＝コード表から。種別＝手入力（DDはリスト受領後） | §8.2・浜田 |
| D-29 | 単位DD共通: ㎡/式/回/人/日/箇月/－。給与初期は箇月 | §7.1a・浜田 |
| D-30 | 給与手当は総括で追加・削除可。最低1行。消費税は－ | §8.2・浜田 |
| D-31 | 総括最下部: ①⑧⑨＋原価区分計＋対①の率（小数1位・①＝0なら率0） | §8.2・Q12 |
| D-32 | 対①の率は小数第1位まで。①＝0なら0 | §8.2・Q12 |
| D-33 | 工事基本情報は736ベース＋不足項目追加（日数等） | §7.0・浜田 |
| D-34 | 工期日数＝竣工−着手（`AC4=U5-U4`）。手入力なし | §7.0・浜田・Excel正 |
| D-35 | 金額＝数量×単価（ROUNDなし）。数量小数可・金額は整数表示 | §15.3・X2・Excel正 |
| D-40 | 金額は常に自動計算。1円差は作成者が数量・単価で調整（運用） | §7.5 X1 |
| D-41 | 計ラベル区別: 請負・施工/保安計 vs 原価・施工/保安計／給与計 | §7.5 X5 |
| D-42 | 給与の消費税－のとき税込も－表示 | §7.5 X7 |
| D-43 | 消費税率の正本は総括原価行（内訳は税抜積み上げ） | §7.5 X4 |
| D-44 | 単位混在ブロックの総括投影は 式×1×税抜合計（編集不可） | §7.5 Q8 |
| D-45 | 種別・計算基準・備考は総括限定（内訳へ非逆流） | §7.5 Q9 |
| D-46 | 新規請負は施工1行・保安1行 | §7.5 Q11 |
| D-47 | 対①率＝金額÷①。①＝0なら率0 | §7.5 Q12 |
| D-48 | 薄い赤は請負と同型を給与・原価にも適用 | §7.5 Q13 |
| D-49 | 基本情報必須は736と同じ（工事コード・版種別・安衛則88条） | §7.5 Q14 |
| D-50 | 内訳明細UX U1〜U8（作成者ブロック追加・工種相互反映＋候補・取引先＝データマスタJ・名称規格＝H/C＋手入力可・全角カナ・単位共通・単価空→金額空・明細初期1行） | §8.5 |
| D-64 | 取引先・名称規格はExcel `データマスタ` 参照。当面コンボ（手入力可）。将来はリスト整備→選択中心 | §8.5 U3/U4 / C-U18–19 |
| D-51 | 新規内訳はブロック0から開始 | §8.5 U9 |
| D-52 | 名称・規格は3列フラット・1列目空可＋内部 `name_spec_group`（現場経費型） | §8.5 U10 |
| D-53 | 数量未入力 → 金額も空白（計算は数量×単価の両方ありのときのみ） | §8.5 U11 |
| D-54 | 内訳ブロック明細は削除後も最低1行 | §8.5 U12 |
| D-55 | 名称規格グループは1列目値＋続く空1列目行への自動継承 | §8.5 U13 |
| D-56 | 並び替え後も内訳№を表示順に詰め直す | §8.5 U14 / §8.3 |
| D-65 | 内訳№はシステム入力工種の左に自動表示（Excel BE非踏襲） | §8.3 / U22 |
| D-66 | ブロック／明細の追加・削除・入替可。フッタ行は固定（案A＝U23） | §8.5 U23 / C-U20 |
| D-67 | 明細入替後は U13 グループ継承を再計算（1列目ブランク対応） | §8.5 U24 / C-U21 |
| D-68 | 小計＝明細合計＋諸経費＋保険料。計＝小計＋法定福利 | §8.5 U25 / C-U22 |
| D-69 | コンボ欄（候補＋手入力可）は常にリスト色（ごく薄い緑） | §8.5 U26 |
| D-70 | 1列目ブランクは見た目空のまま。グループ名は内部継承のみ | §8.5 U27 |
| D-71 | 保存時: 完全空明細は最大1行・余剰削除。中身なしブロックも削除。№詰め直し | §8.5 U28 / C-U23 |
| D-72 | 区分（施工／保安）は取引先の左。未入力は一時保存・保存で赤字警告のみ | §8.5 U29 / C-U24 |
| D-73 | フッタ行（諸経費〜計）は薄い赤の対象外 | §8.5 U30 / C-U25 |
| D-57 | 同一合計はブロックあたり1ソース。参照経路で金額が変わらない。Excel二重参照は非踏襲 | §8.5 U15 |
| D-58 | 内訳単位＝`㎡`/`式`/`回`/`人`/`日`/`箇月`/`－`/`缶`/`枚`/`％` | §8.5 U16 |
| D-59 | 内訳明細の薄い赤は請負と同型（名称系入力後・単位/数量/単価空）。フッタ行は対象外 | §8.5 U17 / U30 |
| D-60 | #REF!非移植。明細金額は数量×単価（両方ありのときのみ） | §8.5 U18 |
| D-61 | ％行の金額＝**単価×％**（数量欄＝率、式は単価×数量÷100）。決定事項として残置 | §8.5 U19 |
| D-62 | ブロック初期セット＝常にフルフッタ（明細1→諸経費→保険料→小計→法定福利→計）。不要行は金額ブランク。諸経費・保険料・法定福利は手入力。小計・計は自動 | §8.5 U20 / C-U14–16 |
| D-63 | セル背景の種別色（案A）: 自動ごく薄い青／手入力ごく薄い黄／リストごく薄い緑。**総括・請負・内訳・予実に共通**。薄い赤は不足指摘優先 | §8.5 U21 / C-U17 |
| D-36 | 原価数字の正は内訳。総括は表示＋限定項目 | §7.4 B1 |
| D-37 | 工事コード枝番は基本情報に持つ（任意。空なら本体のみ表示） | §7.0・B3・Q10 |
| D-38 | 未入力赤でも警告のうえ保存可 | §7.4 B4 |
| D-39 | **総括ラウンド CLOSED**（2026-07-20）。矛盾X/Q解消済。次=内訳 | §7.4–7.5 |
| D-74 | **内訳ラウンド CLOSED**（2026-07-20）。U1–U30合意。次=予実（夜） | §8.6 |
| D-03 | ⑧＝施工計＋保安計＋給与手当計 | §15.4・`AM90=SUM(AM75,AM83,AM89)` |
| D-04 | 税抜・整数円 | §15.3 |
| D-05 | 内訳＝工種ブロック、追加可、総括へ反映、削除時総括連動、№連番 | §8.2–8.3 |
| D-06 | 給与手当＝総括のみ行追加・削除（内訳なし） | §8.3・D-30 |
| D-07 | 予算0の消化率は「—」 | §15.7 |
| D-08 | 試作先 Space56・アプリ名「実行予算書作成支援ツールver02」 | §15.8（未作成） |
| D-09 | 代表案件仮置き 2423101／田立線路橋 | §15.9 |
| D-10 | 内訳ブロック初期セット **No.1–42**（名称・区分） | Excel `内訳!BE` × `総括!AZ` 突合済 |
| D-11 | 給与手当初期行（現場代理人・監理技術者・工事担当者①②②） | Excel 総括84–88 |
| D-12 | 現行736/735は触らない | §4 |
| D-13 | 版確定直後不変、実績は工事帰属 | §9–10 |

**内訳ブロック初期セット（Excel正・No.1–42）**  
施工 No.1–35（材料費…交際費）、保安 No.36–42（工事安全専任管理者賃金…その他保安費）。  
工種番号が空／総括参照のみの行（例: 軌道工事・調査設計費）はコード未設定のまま保持（予想で埋めない）。

### 20.2 予想で作成した部分（実装・仕様に使うが CONFIRMED ではない）

| ID | 予想内容 | なぜ予想か | 試作での扱い |
|---|---|---|---|
| A-01 | 消化率の％表示は小数第1位（対①率と同じ） | 浜田は対①率を小数1位と確定。予実詳細は内訳確定後に再開 | 予実ラウンドで再確認可 |
| A-02 | （解消→U19）諸経費・法定福利も％行は単価×数量÷100 | — | — |
| A-03 | アプリ分割は「1アプリ＋タブ」を仮置き | §11.1 OPEN | 実装GO前に再確認 |
| A-04 | 版メタ（変更理由・確定者）は必須に仮置き | §15-14 OPEN | 最小セットで試作 |
| A-05 | 実績の訂正理由は初回は任意 | §15-17 OPEN | 登録時予算版のみ必須 |
| A-06 | マスタは735読取＋コード表シート由来を候補表示 | 735変更禁止・登録はGO後 | 候補一覧のみ |
| A-07 | UI見た目は736系を踏襲 | デザイン最終仕上げは対象外 | 機能優先 |
| A-08 | （解消→U14）並び替え時も内訳№を詰め直す | — | — |
| A-09 | （解消→D-18）請負単位DD | — | — |
| A-10 | （解消→D-19）未入力赤の発動条件 | — | — |
| A-11 | （解消→D-19）初期空行は赤なし | — | — |
| A-12 | （解消→D-23）請負税込併記は撤回 | — | — |
| A-13 | （解消→D-24）計算基準／備考分割 | — | — |
| A-14 | （解消→D-26）新規行消費税初期10％ | — | — |
| A-15 | （解消→D-28）種別手入力 | — | — |
| A-16 | （解消→D-31）サマリー構成 | — | — |
| A-17 | （解消→D-34）工期日数＝竣工−着手 | — | — |

### 20.3 依頼者確認リスト（こちらでは決められない／Excel不能）

| ID | 確認事項 | ブロッカー |
|---|---|---|
| R-01 | （解消）請負＝税抜のみ／原価＝消費税＋税抜＋税込 | §7.1a D-23・D-25 |
| R-02 | （解消→D-35）端数＝小数第1位四捨五入。1円差はシステム値優先 | §15.3・B2 |
| R-03 | 各明細の施工／保安／給与への帰属ルール（またがり） | 行見出し以外の規則不明 |
| R-04 | （方針→U18）`#REF!` は移植しない。明細金額は数量×単価。壊れた式の復元はしない | §8.5 U18 |
| R-11 | **諸経費**の正式自動計算式（現状は手入力で試作） | 式不明・`#REF!`混在 |
| R-12 | **法定福利費**の係数 `0.9×0.1515` と基数（現状は手入力で試作） | 式は読めるが業務正かは依頼者 |
| R-13 | **各種保険料**の正式自動計算（現状は手入力で試作） | Excelはラベル行中心 |
| R-05 | 工種番号が空の行の正式コード | Excelで null／参照のみ |
| R-06 | 代表案件の期待検算値の最終承認 | 創作禁止のため依頼者またはExcel保存値の公式採用宣言 |
| R-07 | コード表・データマスタの未参照候補の処置 | §12 |
| R-08 | 実績の作成・修正・削除権限と締め | §15-4,5 |
| R-09 | 予算確定の権限・警告 | §15-9 |
| R-10 | App 678 連携の要否・粒度 | 初回試作外だが将来方針 |

### 20.4 次アクション（実装GO前・こちらで継続可）

1. No.1–42 のフィールド対応表（総括行 ↔ 内訳ブロック ↔ 区分）をリポに起こす。  
2. 画面モック／データ対応表の下書き（予想箇所は A-* を注記）。  
3. 依頼者確認は R-* だけを1枚にまとめて提示。  
4. **実装GO・アプリ作成は浜田明示後**（§15.8）。
