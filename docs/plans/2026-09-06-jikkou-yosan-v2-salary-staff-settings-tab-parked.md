# 756 見積・社員リスト設定タブ（駐車・案）

**日付**: 2026-09-06  
**状態**: **未実装**。浜田が「案出して」と言ったら本ファイルを出す。実装 GO まではコード・deploy しない。  
**きっかけ**: 給与手当の氏名（G0 **S1** / U列 32人）を、今後見積で使える社員リストとして設定できるようにしたい。  
**正本の位置**: 本ファイル。統括正本 `docs/plans/2026-09-05-jikkou-yosan-v2-summary-tonight-decisions.md` は本日分クローズ。G0 S1 から本ファイルへポインタ。  
**736**: 不触。**757 customize**: 不要。予実に按分しない。

---

## 0. いまの確定（実装しない理由）

- 2026-09-06 朝の統括実装は **完了・目視OK**。実行予算は本日終わり。
- 設定タブは **今日やらなくてよい**。依頼があればすぐ案を出す。
- U は「ゆくゆく作業員設定タブへ集約」（統括正本 §3）。今回は移していない。

---

## 1. v1 の範囲（案・推奨）

**v1 = 給与手当の氏名リストだけ。**

現行は JS 埋め込み `SALARY_PERSON_MASTER`（32人、姓＋全角空白1＋名、listOnly、祖父は G0 §16）。これを **テナント共通マスタ** にして、設定タブから追加・無効化できるようにする。

給与表はマスタを **listOnly で読む**。756 の工事レコードに社員名簿を持たない。

---

## 2. 混ぜない判断（案を出すときの3点）

浜田が「案出して」と言ったとき、先にこの3点を出す。**推奨はすべて no。**

| # | 混ぜるか | 推奨 | 理由 |
|---|---------|------|------|
| 1 | 給与 **T 名称**（社員助勢等 11件） | **no** | 名称は手当種別。社員リストではない |
| 2 | 内訳の氏名 | **no** | 内訳は協力会社側。S1 と別系統 |
| 3 | App **595** 従業員台帳 | **no** | 会社の人事台帳。見積の給与氏名と目的が違う。同期すると壊れる |

v1 で混ぜると、設定タブが「社員マスタ」ではなく「全部の人」になり、給与 listOnly が壊れる。

---

## 3. 置き場（案・推奨）

**756 のプロジェクトレコードに置かない。** 工事ごとに名簿が分かれる。

推奨: Space **56** に新アプリ `実行予算_社員マスタ`（仮）。

| フィールド | 内容 |
|-----------|------|
| 氏名 | 姓＋全角空白1＋名（U と同じ） |
| 有効/無効 | 無効は給与候補から外す。既存行の祖父は G0 §16 |
| 並び | 給与ドロップの順 |
| 備考 | 任意 |

初期データ = 現行 32人。JS 埋め込みは廃止。

代替案（非推奨）: 735 リストマスタへ寄せる。給与専用アプリの方が権限を切りやすい。

---

## 4. 756 UI（案）

現行シェルタブ: ヘッダ / 休日 / 統括 / 内訳 / 予実 / 版。  
**設定** タブを足す（作業員設定、という名前でも可。v1 は社員リストのみ）。

- 編集できる人: 管理者・作成者側
- 一般: 読取（給与で使う）
- 印刷ポータルに設定 UI を出さない（`jy2-pr-*` / `#jy2-print-portal`）
- 画面入力表の文字揃えは変えない

---

## 5. やらない（v1）

- 予実タブの作り直し・按分
- 請負の全面・請負計／給与計の備考
- 736、閉済9件、688 WBGT 以外、677–679
- 757 customize deploy
- 内訳氏名のマスタ化
- 595 との自動同期

---

## 6. 実装順（GO 後）

1. 浜田が判断3点を確認（推奨 no）
2. 新アプリ or 735 の器
3. 32人シード
4. 給与 U をマスタ参照に切替（listOnly 維持）
5. 756 設定タブ
6. JS 埋め込み削除
7. テスト → preflight → deploy 756（マスタアプリも）

コード候補: `customize/jikkou-yosan-v2-app1/` と `scripts/lib/jikkou-yosan-v2/contract-salary-model.mjs`。
