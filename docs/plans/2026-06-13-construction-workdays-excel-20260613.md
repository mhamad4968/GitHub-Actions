# 工事稼働日数算出 — Excel 20260613 準拠（688/687）

**正本ツール**: `C:\tmp\稼働日数算出ツール\稼働日数算出ツール20260613.xlsx`  
**前提 SPEC**: `docs/plans/2026-05-17-construction-workdays-spec-v1.md`（Space 56・687/688 構成）  
**更新日**: 2026-06-23  
**本番 BUILD（688）**: `2026-06-22-688-print-5yr-scale-up`（revision 64）— §11 改修デプロイ前

---

## 1. 入力・基準年

| 項目 | 仕様 |
|------|------|
| **見積作成年** | 必須。687 フィールド `estimate_year`（NUMBER）。688 の主入力。 |
| 着工日・完工日 | **不要**（687 では任意。688 では非表示）。 |
| 休日・暦日 | **見積作成年**の各月全日を暦日 C とする。 |
| 手動休日 | **土曜**（再算出で自動上書き・手修正可）・GW・夏休み・年末年始（月別サブテーブル `holiday_manual`） |
| 自動休日 | **日曜**（暦から自動。祝日と重なる日は祝日のみ計上） |

## 2. ※1 気象日数（過去5年）

| 項目 | 仕様 |
|------|------|
| 参照期間 | **見積作成年 Y の直前5年**（Y−5 〜 Y−1）。例: 2026 → 2021〜2025 |
| 足場 | 日別最大風速 **≧10 m/s** の月別日数 → 5年平均 |
| 塗装・休日 | 日別降水量 **≧10 mm**（気象庁「降水量の日合計 (mm)」）の月別日数 → 5年平均 |
| 表の月列 | **常に 1月〜12月**（見積年の暦月） |

算出式は Excel 20260613 と同一（`calcWorkdaysExcel20260613` / Option A 祝日マスタ）。

## 3. 過去5年参照表（打合せ・監査用）

688 タブ「過去5年(風速)」「過去5年(降雨)」に **Excel と同じ全閾値表** を表示。

| 種別 | 閾値 |
|------|------|
| 風速 | ≧10 / 15 / 20 / 30 m/s |
| 降雨 | ≧1 / 10 / 30 / 50 / 70 / 100 mm |

- 列は **見積作成年に対応する過去5年** のみ表示。
- 月列ヘッダー下の「2026年」等の年ラベルは **表示しない**。
- 該当日が0件の月・閾値も **0日** として登録（不足年警告を出さない）。

## 4. 参照データの構成

| 層 | 内容 |
|----|------|
| 組込 JSON | `scripts/data/workdays-5yr-omiya.json`（大宮地区・2018〜2025 等） |
| 687 保存 | 風速・降雨の **日別行**（`wind_data` / `rain_data` サブテーブル） |
| 実行時 | 組込 JSON ＋ 保存済み日別行をマージして `ref5yr` を再構築 |

**保存しない場合**: CSV 取込結果はセッション内のみ。再読込で消える。  
**保存した場合**: 案件読込時に日別行から再マージされ、過去5年表が再現される。

## 5. CSV 取込（688 UI）

| 項目 | 仕様 |
|------|------|
| ファイル | **風速CSV** と **降雨CSV** を **別々に** 取込（両方必要） |
| 形式 | 2列（日付, 値）。Shift_JIS / UTF-8 自動判定 |
| マージ | 組込参照表に **同じ年は上書き・新年は追加** |
| 全閾値 | 1回の取込で当該種別の全閾値ブロックを更新 |
| 再算出 | 取込後に自動実行（≧10m/s と ≧10mm が揃っていること） |

**例**: 見積作成年 2016 → 2011〜2015 の日別 CSV を風速・降雨それぞれ取込 → 表に 2011〜2015 が反映。

リポジトリ一括更新（開発用）:

```bash
npm run workdays:import-jma-csv
```

## 6. 687 フィールド追加

| コード | 種別 | 備考 |
|--------|------|------|
| `estimate_year` | NUMBER | 見積作成年（必須） |
| `start_date` / `end_date` | DATE | 任意（後方互換） |

追加スクリプト: `scripts/workdays-add-estimate-year-687.mjs`

## 7. ビルド・デプロイ・ゲート

**deploy は AI が実行**（浜田 GO 後も `npm run deploy:*` を人に依頼しない — TSB-024 / §52）。

```bash
npm run workdays:build-desktop:688   # WORKDAYS_BUILD=… を設定可
npm run cio:preflight:688 -- --note "…"   # 45 分以内に deploy
npm run deploy:688   # workdays-deploy-gate + cio-deploy-preflight-guard + upload
```

`deploy:688` は **機械連鎖**（抜け落ち防止）:

1. `workdays-verify-built-ui` — `desktop.ui.js` と `desktop.js` の BUILD 一致
2. `workdays-calc-gate` — Excel 回帰
3. `cio-deploy-preflight-guard` — preflight スタンプ＋（既定）live-schema
4. `deploy-customization.js` — 本番反映＋ `cio-live-builds.json` 更新

セッション締め: `npm run verify:cio-deploy-ledger-gate`（R21 — registry ↔ repo ↔ `kintone-apps.md` 機械表）。

Runbook: `docs/runbooks/workdays-deploy-checklist.md`

## 8. 計算正本

- `scripts/workdays-calc-core.mjs`
  - `pastFiveYearsForEstimate`
  - `build5yrMonthlyAverages`
  - `calcWorkdaysBundleForEstimate`
  - `mergeDailyCsvIntoRef5yr` / `aggregateDailyToMonthlyCounts`
- 回帰: `scripts/workdays-calc-gate.mjs`（Excel 2023 足場・塗装 ±2.5 日）
- PDF 丸め（工期設定資料 p.94）:
  - `roundWorkdaysPdf1` — 小数第2位四捨五入・小数第1位止め
  - `calcRainHolidayRatePdf` — 塗装の雨休率（ダブり補正・率は整数％）

## 9. 施工主報告用印刷（688 UI）

688 詳細画面の **「施工主報告用印刷」** ボタン（683 型: 非表示 `#wd688-print-portal` + `window.print()`）。

### 9.1 出力セクション（Excel 報告用ブック準拠・順序固定）

画面上の Excel 相当タブは 5 種。印刷 HTML は **5 シート**（`.wd688pr-sheet`）に分割し、過去5年は **降雨・風速を別枚**とする。

| # | 印刷ページ | シート相当タイトル | 内容 |
|---|-----------|-------------------|------|
| 1 | 1枚目・表 | 工事稼働日管理 (塗装) | ヘッダ（工事名・見積年・観測地点・結果日数）＋年間表＋コメント欄＋脚注＋12か月カレンダー |
| 2 | 1枚目・裏 | 工事稼働日管理 (足場) | 年間表＋コメント欄＋脚注＋12か月カレンダー |
| 3 | 2枚目・表 | 工事稼働日管理 (休日) | 年間表＋コメント欄＋脚注＋12か月カレンダー |
| 4 | 2枚目・裏 | 過去5年月別降雨日数 | 降雨6閾値（3列×2行） |
| 5 | 3枚目・表 | 過去5年月別風速日数 | 風速4閾値（4列×1行） |

各稼働日セクション: タブ行 → 青見出し → 年間集計表 → 100%超コメント欄（該当時）→ 脚注 → 月別カレンダー。

### 9.2 表示・丸め（PDF p.94）

| 項目 | 丸め |
|------|------|
| 降雨・降雪等日数 / 風速日数 | 小数第1位（第2位四捨五入） |
| 休日数とのダブり日数 | 小数第1位 |
| 稼働可能日数 | 小数第1位 |
| 雨休率・不稼働率 | 整数％（小数以下四捨五入） |

塗装は `calcRainHolidayRatePdf`、足場は `windPdfMonth` / `windPdfYear`（`roundWorkdaysPdf1` 適用）。年列も月列と同じ丸めを用い、浮動小数点合計の生値は表示しない。

### 9.3 印刷レイアウト（2026-06-22 確定・浜田確認済）

- `@media print` で **A3 横**（`@page { size: A3 landscape; margin: 2mm; }`）・**5ページ**（**両面印刷時は用紙3枚**）
  - **1ページ目**（1枚目・表）: ヘッダ＋工事稼働日管理（塗装）
  - **2ページ目**（1枚目・裏）: 工事稼働日管理（足場）
  - **3ページ目**（2枚目・表）: 工事稼働日管理（休日）
  - **4ページ目**（2枚目・裏）: 過去5年（降雨）— 下記 9.3.2
  - **5ページ目**（3枚目・表）: 過去5年（風速）— 下記 9.3.3
- プリンタ設定: **A3・横向き・両面（長辺とじ）** を推奨
- 印刷フッター（印刷日 / BUILD / ツール名）は **出力しない**（末尾空白ページ防止）
- 表題・セクション順序は Excel 報告用と同一（変更時は浜田確認）

#### 9.3.1 稼働日3面（塗装・足場・休日）

| 項目 | 仕様 |
|------|------|
| カレンダー配置 | **6列×2行**（1〜6月 / 7〜12月） |
| カレンダー下の月別内訳表（暦日等） | **印刷時表示**（`.wd688pr-cal-stats-wrap`） |
| 文字サイズ（印刷） | 集計表 **12.5pt**・タイトル **19pt**・カレンダー日付 **10pt**・年見出し **14pt**（120% スケール — 1面収まりと可読性のバランス、2026-06-22 浜田確認済） |
| 改ページ | 各 `.wd688pr-sheet-work` の末尾で `page-break-after: always` |

#### 9.3.2 過去5年・降雨（1枚）

| 項目 | 仕様 |
|------|------|
| 降雨 | 6閾値表を **3列×2行** グリッド |
| タブ行 | 印刷時 **非表示**（見出し h2 のみ） |
| 文字サイズ（印刷） | 表 **14pt**・閾値見出し **13pt**・セクション見出し **22pt**・メタ行 **14pt**（余白活用の拡大、2026-06-22 浜田確認済） |

#### 9.3.3 過去5年・風速（1枚）

| 項目 | 仕様 |
|------|------|
| 風速 | 4閾値表を **4列×1行** グリッド |
| タブ行 | 印刷時 **非表示**（見出し h2 のみ） |
| 文字サイズ（印刷） | 表 **14pt**・閾値見出し **13pt**・セクション見出し **22pt**・メタ行 **14pt**（降雨面と同一スケール） |

### 9.4 参照

- Excel: `C:\tmp\稼働日数算出ツール\報告用\20260422_稼働日数算出(2025年度).xlsx`
- PDF: `C:\tmp\稼働日数算出ツール\報告用\工期設定不稼働率算出.pdf`（p.94 丸め規則）
- 実装: `customize/688/desktop.ui.js`（`buildFullClientReportHtml` / `buildWorkdaysMgmtPrintSection` / `buildPrint5yrSheet` / `paintReportPrintStylesheet`）

## 10. 不稼働率 100%超の表示・コメント欄（688 UI）

Excel 準拠の不稼働率は **（休日＋天候−ダブり）÷ 稼働可能日数** のため、分子が分母を超える月は **100%超**（例: 8月塗装 18.32 ÷ 12.68 ≒ 144%）。計算バグではない。

### 10.1 月次表

| 項目 | 仕様 |
|------|------|
| 雨休率・不稼働率 | **実際の％を表示**（100%超も例: 144%）。100%超は **赤文字強調**（`wd688-rate-over`） |
| 休日タブ | `renderExcelTransposedTable(rows, 'holiday')`（降雨 0・稼働＝暦日−休日） |

### 10.2 コメント欄（表とは別ブロック）

| 項目 | 仕様 |
|------|------|
| 配置 | 足場・塗装・休日タブの **表直下**（`buildOver100CommentPanel`） |
| 表示条件 | 再算出後、当タブで不稼働率 100%超の月があるとき **自動表示** |
| 保存 | **kintone 非保存**・手入力不可（参考用 UI のみ） |
| 印刷 | 施工主報告用印刷にも同コメント欄を含む |

### 10.3 月別コメント文面（`buildOver100MonthNote`）

塗装・足場（天候あり）の例:

> 5月: 休日14日（土日10・祝3・GW1）＋降雨4.2日（ダブり控除後・不稼働16.3日）が稼働可能14.7日を上回ります。（不稼働16.3÷稼働14.7≒111%）

| タブ | 天候ラベル | 式の分子 |
|------|-----------|---------|
| 塗装 | 降雨 | 休日＋降雨−ダブり |
| 足場 | 風速 | 休日＋風速−ダブり |
| 休日 | （なし） | 休日のみ |

末尾は常に **（不稼働÷稼働≒％）**。休日内訳は `holidayPartsSummary`（土曜・日曜・祝・GW 等）。

### 10.4 実装参照

- `customize/688/desktop.ui.js` — `buildOver100MonthNote` / `buildOver100CommentPanel` / `renderMonthlyTable`

---

## 11. 688 UI 改修（2026-06-23・浜田合意）

利用者フィードバックに基づく改修。**実装 GO**: 2026-06-23（仕様コミット後に着手）。

### 11.1 休日数 — 土曜／日曜の分割

| 項目 | 仕様 |
|------|------|
| 対象タブ | 足場・塗装・休日（休日データは共通） |
| 旧仕様 | 「土　曜・日　曜」1行で土日を自動集計 |
| **土曜** | 月別 **手入力欄**（`holiday_manual.hm_saturday`） |
| 土曜の再算出 | **毎回自動計算値で上書き**（登録者が必要なら手修正） |
| 土曜の自動値 | 見積作成年の各暦月における **土曜のうち祝日でない日** の日数 |
| 土曜の注意 | 再算出直後、休日表付近に **赤字** で「自動計算値のため手直しが必要な場合あり」を表示 |
| **日曜** | **自動算出**（手入力不可） |
| 日曜の自動値 | 各暦月の **日曜のうち祝日でない日** の日数 |
| 祝日との重複 | 土曜・日曜いずれも **祝日と重なる日は祝日のみ**（二重計上しない） |
| 休日計 D | 土曜（手入力）＋日曜（自動）＋祝日＋GW＋夏休み＋年末年始 |

### 11.2 不稼働率・雨休率の表示

| 項目 | 仕様 |
|------|------|
| 月次表・印刷 | 100%超も **実％** を表示（§10.1 を 2026-06-23 に更新） |
| 強調 | 100%超は **赤文字** を維持 |
| コメント欄 | §10.2 の 100%超自動コメントは **維持** |

### 11.3 CSV 取込ボタン配置

| 項目 | 仕様 |
|------|------|
| CSV→風速 | 気象庁手順ブロック **① 風速（足場・※1）** の説明直下 |
| CSV→降雨 | 同 **② 降雨（塗装・休日・※1）** の説明直下 |
| 見た目 | 既存ボタンより **やや薄い色** で区別 |
| 画面上部 | 横並びの CSV ボタン行は **廃止** |

### 11.4 観測地点（CSV 自動・変更不可）

| 項目 | 仕様 |
|------|------|
| 地点の取得 | 気象庁 CSV ヘッダ（3行目付近の地点名）から **自動抽出** |
| 正規化 | `OBS_OPTIONS`（687 `obs_location`）へマッピング。未対応名はエラー |
| 風速・降雨の一致 | 2種類 CSV の地点名が **異なる場合はエラー**（取込しない） |
| CSV 未取込 | **「未設定」** 表示。プルダウン **なし**（手選択不可） |
| CSV 取込後 | 地点名を **表示のみ**（変更不可） |
| 既存案件読込 | 687 に保存済みの地点を **表示のみ**（A）。CSV 再取込時に地点照合 |
| 手順文 | 「地点を手で選ぶ」記述は **削除・修正** |

### 11.5 地点備考の廃止

| 項目 | 仕様 |
|------|------|
| `obs_location_note` | **687 フィールド削除**（UI・印刷・保存ともに不使用） |

### 11.6 687 フィールド変更

| コード | 変更 |
|--------|------|
| `holiday_manual.hm_saturday` | **追加**（NUMBER・月別土曜） |
| `obs_location_note` | **削除** |

スクリプト: `scripts/workdays-add-fields-687.mjs`（または同等）で本番反映。

### 11.7 実装・デプロイ

```bash
npm run workdays:build-desktop:688
npm run workdays:calc-gate
npm run cio:preflight:688 -- --note "688 workdays 2026-06-23"
npm run deploy:688
```

正本実装: `scripts/workdays-calc-core.mjs`（`holidayBreakdownInRange` 分割） / `customize/688/desktop.ui.js`

---

## 12. 猛暑日（WBGT）参考表示（2026-07-10 · 部分 GO）

**詳細正本**: [`2026-07-10-688-wbgt-heat-reference-spec.md`](2026-07-10-688-wbgt-heat-reference-spec.md)

| 項目 | 内容 |
|------|------|
| **目的** | 塗装・足場・休日の月別表最下行 + 過去5年タブ + 印刷に **猛暑日数（参考）** |
| **計算** | WBGT≥31 の **時間数 ÷ 8h** → 過去5年 **月平均** |
| **データ** | [環境省 WBGT 実測 DL](https://www.wbgt.env.go.jp/wbgt_data_download.php) · `CSV→猛暑日（WBGT）` |
| **厳守** | **稼働可能日数・不稼働率・再算出結果に含めない** |
| **オプション** | 画面表示 ON/OFF · 印刷 ON/OFF（既定 OFF） |
| **688 保留** | **本件のみ部分解凍** — 他改修は触らない |

AIチームレビュー: **問題なし（2026-07-10）** — implement は commit/push 後に浜田確認。
