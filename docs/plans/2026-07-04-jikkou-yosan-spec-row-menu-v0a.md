# 実行予算書 736 — 仕様明細 ⋮ 行メニュー（Phase 0a）

**日付**: 2026-07-04 JST  
**浜田 GO**: 別システム参考・段階導入 — **簡単で影響小から**  
**親 SPEC**: [`2026-06-18-jikkou-yosan-spec.md`](2026-06-18-jikkou-yosan-spec.md) §9.2  
**AI チーム合議**: 2026-07-04 — 影響小・**GO**

---

## 1. 目的

仕様明細（①）の行操作（**下に追加・削除**）を **⋮ コンテキストメニュー**に集約し、操作の見通しをよくする。  
**新ロジックは追加しない**（既存 `insertSpecRowAfter` / `spec_lines.splice` をそのまま利用）。

---

## 2. スコープ（Phase 0a）

| 項目 | 内容 |
|------|------|
| **対象** | 総括表 — **仕様明細（①）** のみ |
| **メニュー項目** | **下に1行追加** / **行を削除** |
| **残す** | 見出し **「＋ 末尾に追加」**（変更なし） |
| **含めない** | 上に追加・🔍・並び替え・テキスト行・原価/材料/外注 |

---

## 3. 技術方針

- UI: 各行 `⋮` → ポップアップメニュー（`data-spec-add-after` / `data-spec-del` は **既存のまま**）
- `readOnly` / ロック時は操作列なし（現行同等）
- deploy 前: `jikkou-yosan:deploy-gate` + ux-gate 不変条件追記

---

## 4. 受け入れ

| # | 操作 | 期待 |
|---|------|------|
| 1 | ⋮ → 下に1行追加 | 行追加・薄黄・① 合計再計算 |
| 2 | ⋮ → 行を削除 | 行削除・① 合計更新 |
| 3 | 版確定/ロック | ⋮ 非表示 |
| 4 | 差分 ON | diff 正常（削除 0 件誤爆なし） |
| 5 | sticky / 印刷 UI | ux-gate OK（先祖返りなし） |

---

## 5. ロードマップ（後続）

| Phase | 内容 |
|-------|------|
| **0b** | 上に1行追加（仕様①） |
| **0c** | 他表への横展開 |
| **1+** | 🔍 マスタ検索・並び替え・テキスト行 |

**BUILD**: `2026-07-04-736-spec-row-menu-v0a-fix`（rev **159** — ⋮ クリック修正: overflow + JS トグル）
