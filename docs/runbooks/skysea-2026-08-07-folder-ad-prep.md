# SKYSEA — 2026-08-07 事前準備（共有フォルダー／ADグループ）

**実施日**: 2026-08-07（浜田）  
**いま**: 回答待ち（実PC配信・既存GPO変更・GPO新規作成は **まだしない**）  
**正本**: plan §11.4 / §11.6 / §11.12 · `skysea-2026-08-stage-b-prep.md`

---

## 8/7 に「作るもの」（これだけでOK）

### 1. 共有フォルダー（必須）

| # | 作るもの | 内容 |
|---|----------|------|
| 1-1 | **配布用フォルダー** | 例: `\\<ファイルサーバ>\skysea$\Client\`（パスは浜田が確定） |
| 1-2 | **ログ用フォルダー**（別系統） | 例: `\\<ファイルサーバ>\skysea$\Logs\` — インストーラー用と **分ける**（§11.12） |
| 1-3 | **NTFS／共有権限** | ・`Domain Computers` … **読取のみ**（Client）／ログは後でPCごと制御可 ・管理者 … 書込可 ・**Everyone の広い権限は禁止** |
| 1-4 | **インストーラー本体の配置** | 約101MB。管理機 **17.210.07j** 対応版のみ（SKY回答で最終確定。未確定なら「配置予定フォルダだけ先に作る」でも可） |
| 1-5 | **SHA-256 メモ** | 正規ファイルを置いたらハッシュを1行メモ（メモ帳で可）。試験前に再計測してもよい |

**やらなくてよい（8/7）**
- 各PCへのコピー・実行
- 共有から直接インストールの試験

---

### 2. ADセキュリティグループ（必須）

| # | 作るもの | 内容 |
|---|----------|------|
| 2-1 | **新規セキュリティグループ** | 名案（仮）: `SG-SKYSEA-Deploy-2026` — 浜田が最終名を決めて作成 |
| 2-2 | **種類** | セキュリティ／Global（ドメイン） |
| 2-3 | **メンバ** | **空のまま**、または試験用に指定1台だけ（8/7は空推奨） |
| 2-4 | **OU** | PCを別OUへ **移動しない**。グループだけで絞る |

**やらなくてよい（8/7）**
- 123台を一括メンバ追加（配信GO後）
- 既存GPOの変更
- **新規GPOオブジェクトの作成**（実装GO・SKY回答後）

---

### 3. 任意（あると楽・必須ではない）

| # | 内容 |
|---|------|
| 3-1 | フォルダー実パス・グループ名をチャットまたはメモに1行残す（AIが手順書をパス実名に更新） |
| 3-2 | 受信したSKY社回答メールを保管（届いていれば） |

---

## 8/7 完了チェック（浜田用・短）

- [ ] `\\…\skysea$\Client\`（または同等）を作成した  
- [ ] `\\…\skysea$\Logs\`（または同等）を分けて作成した  
- [ ] Domain Computers=読取／管理者=書込／Everyone無し  
- [ ] （任意）インストーラー配置＋SHA-256メモ  
- [ ] ADグループ `SG-SKYSEA-Deploy-2026`（または確定名）を **新規作成**・メンバ空  
- [ ] 既存GPOは触っていない／新規GPOは作っていない  
- [ ] 実PCへは何も配信していない  

完了したらチャットに「8/7準備完了」＋実パス・グループ名を1行ください。

---

## いまやらないこと（再掲）

| 禁止 | いつ解禁 |
|------|----------|
| 実PC試験・本番配信 | SKY社回答 **かつ** 実装GO |
| 既存GPO変更 | しない（新規専用GPOのみ・GO後） |
| 新規GPO作成 | 実装GO後 |
| 123台のAD一括追加 | 試験1台→段階展開の都度 |
| App 674 一括書込 | ドライラン＋バックアップ＋GO後 |
