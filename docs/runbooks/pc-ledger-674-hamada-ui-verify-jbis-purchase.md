# 674 PC台帳 — 浜田 CEO 目視チェック（依頼時のみ）

CIO が手順・証跡を先に用意し、浜田 CEO が **依頼時のみ**画面で確認する。本番: [674](https://jbis-kintone.cybozu.com/k/674/)。

## 前提

- customize **BUILD** `2026-08-06-674-jbis-max-plus-one`（live rev **266** 付近）。購入3フィールドはフォーム **rev 197** 以降。
- 採番仕様（§4.3.1・2026-08-06 浜田 GO）: **空き番（歯抜け）は使わない**。次番 = 廃棄・取消以外の有効 `pc_name` から読んだ **JBIS / S-JBIS 連番の max + 1**。個人は 670 **`PC_SERIAL_MIN_PERSONAL_JBIS`**（未設定時 **67**）未満にしない。番兵 **9999** は max 除外。**一覧「次採番」バナーと自動生成は同一式**。**`pc_name` が空の行のみ**自動採番。**登録済み JBIS/S-JBIS 名は変更しない**。**JR** は PC 名手入力のまま。

## 1. 個人 JBIS（max+1）

1. 個人・**`pc_name` 空**の新規または編集画面を開く。
2. **自動採番**（または同等操作）で PC 名が付与されること。
3. 付与名が **JBIS+連番-YYYYMM** 形式で、一覧バナーの **次採番**と **一致**すること（歯抜け再利用はしない。例: 台帳 max=349・番兵9999除外 → **JBIS0350**）。
4. **既に JBIS 名が入っている個人行**で自動採番を触っても **名前が変わらない**こと。

## 2. 共有 S-JBIS（max+1）

1. 共有・**`pc_name` 空**の行で **共有自動生成**を実行。
2. **S-JBIS+連番-YYYYMM** が付与され、一覧バナーの共有次採番と **一致**すること（空き番不使用）。
3. **671** から利用可な共有 M365 が取得できること（「入力内容が正しくありません」等が出ないこと）。
4. **登録済み S-JBIS 名**は自動操作で **上書きされない**こと。

## 3. 購入欄（金額・購入先）

1. 詳細／編集で **購入日の直後**に **購入金額（円）**・**購入先**（大塚商会／FBJ／KDDI／コジマ／ツクモ）・**購入先（その他手入力）** が表示されること。
2. 金額は数値入力、購入先はプルダウン＋その他欄が保存・再表示できること。
3. 一覧・印刷で CEO が必要とする位置にフィールドが見えること（レイアウト rev **197**）。

## 記録（CIO）

- 確認日時・BUILD/rev・3項目の **OK/NG** を `handoff-log.md` または当セッションの報告に1行ずつ残す。
- NG 時は **再現手順**と **コンソール／ネットワーク**の抜粋を添える。
