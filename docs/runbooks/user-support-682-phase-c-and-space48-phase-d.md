# 682 ユーザサポート件数日次 — フェーズ C（グラフ）とフェーズ D（Space 48 ダッシュ）

> **正本仕様**: `docs/plans/2026-05-08-user-support-daily-counts-spec.md`（§5・§5.1・§6.1・§6.2・§9.1）  
> **682 台帳**: `kintone-apps.md`（deploy・BUILD・fileKey）  
> **Space**: [Space 48](https://jbis-kintone.cybozu.com/k/#/space/48)

## 0. CEO 裁量（2026-05-10）

- **浜田 CEO**: 「CIO 判断に任せる」「AI チームで相談して対応」「完了報告は聞く」。
- **CIO**: **§9.1 の C → D** を **4 月実データが存在する前提**で前倒し実行してよい（**B の目視は浜田依頼時**に実施で可）。
- **不可逆・本番**: **682 の `deploy:682`** や **Space の構成変更**は従来どおり **preflight・第2者・証跡**を欠かさない。

---

## 1. フェーズ C — アプリ 682 の月次棒グラフ（§5.1）

**目的**: kintone **標準 UI** で **`日合計`（`day_total`）のみ**の **暦月棒グラフ**を **公開まで**。

### 1.0 REST 一発（CIO 自動・2026-05-10 CEO GO）

- **スクリプト**: `scripts/user-support-682-ensure-monthly-bar-graph.mjs`
- **npm（ローカル・`.env`）**: `npm run 682:graph-monthly:dry-run`（確認のみ）→ **`npm run cio:preflight:682 -- --note "…"`** のあと **`npm run 682:graph-monthly`**
- **npm（CI・Secrets のみ）**: **`npm run 682:graph-monthly:gha`**（preflight + §50-3-8 skip stamp + guard + REST を一括。**workflow から `682:graph-monthly:scheduled` 直接呼び出し禁止**）。
- **自動（GitHub Actions）**: **`.github/workflows/682-graph-monthly-refresh.yml`** — **毎月 1 日 14:00 UTC**（≈ JST 同日 23:00）に **`682:graph-monthly:gha`**。**手動**は Actions の **Run workflow**。**本ワークフローは `environment:` を使わず**、**Repository secrets** の **`KINTONE_BASE_URL` / `KINTONE_USERNAME` / `KINTONE_PASSWORD`**（任意 Basic）のみを参照する（**`kintone-collect` の Required reviewers で止まらない**）。5038 正本: **`docs/runbooks/cio-gha-periodic-5038-stamp.md`**。
- **欠月を 0 で見せる**: kintone **標準グラフのみ**では不可。**`customize/682/desktop.js`**（`BUILD` 参照）が **一覧ヘッダ＋グラフ画面**で REST 集計の **7 暦月 0 埋め棒**を追加。反映は **`npm run deploy:682`**。
- **挙動**: 既存グラフを **GET で全件マージ**し、§5.1 準拠グラフ（`record_date` **MONTH** × `day_total` **SUM**・**`COLUMN`（縦棒）**・**JST 直近 7 暦月**の `filterCond`）が **最新でない**とき **PUT `preview/app/reports.json`** → **`preview/app/deploy.json`** まで実行（同一月・同一設定ならスキップ）。**既存グラフが無いアプリでは安全に追加のみ**。
- **手動 UI**（下記 1〜8）と **同等の設定**になるよう設計（公式 Update Graph Settings API に準拠）。

**手順（SPEC §5.1 と同一・手動または検証用チェックリスト）**:

1. [ ] アプリ **682** を開く → **設定** → **グラフ**。
2. [ ] **集計グラフ**を追加（または既存を編集）。
3. [ ] **種類**: **縦棒**（API 表記 **`COLUMN`**）。
4. [ ] **集計対象**: **`日合計`（`day_total`）**・集計方法 **合計**（相当）。**`am_count` / `pm_count` を系列に入れない**（二重計上防止）。
5. [ ] **グループ（横軸）**: **`記録日`（`record_date`）**・グルーピング **月ごと**（暦月）。
6. [ ] **プレビュー**: 4 月など **実データがある月**に棒が立つことを確認。
7. [ ] **計算フィールドの反映**: 一覧再表示や再保存で `day_total` が期待どおりか確認（§5「注意」）。
8. [ ] **公開**。

**完了の定義**: グラフが **利用者に見える状態**（公開済み）で、**少なくとも 1 暦月**が期待値と整合。

---

## 2. フェーズ D — Space 48 のダッシュ骨組み（§6.1）

**目的**: **主たる閲覧面**を Space 48 に寄せ、**日次一覧・当月合計・MoM（前月比）**の置き場を確保する（詳細レイアウトは §6.1）。

### 2.0 明日の作業スケジュール案（**2026-05-10（日）JST**・CEO／CIO 合意の「明日着手」用）

**前提（今日までに確定済み＝SPEC 正本）**: §6.1（Space 48 主画面・当月合計・MoM 赤青・任意の長期グラフ）、§6.2（欠日／重複の精神）、§9.1 **D**（ダッシュ骨組み）。682 側の **7 暦月 0 埋め棒**は現状 **アプリ一覧ヘッダ**にあり、**Phase D でポータルへ寄せたあと**、一覧上の重複を **折りたたみ／撤去**するのがよい（CIO 後続・SPEC追記可）。**ダッシュで同等表示を浜田が確認できたら**、`customize/682/desktop.js` の **`SHOW_ROLLING_7M_ON_APP682 = false`** → **`npm run cio:preflight:682`** → **`npm run deploy:682`** で **[682](https://jbis-kintone.cybozu.com/k/682/) から 7 暦月棒を外す**（§5.1 正本・`BUILD` 更新）。

**当日の壁（DeepSeek 突合メモ）**: (1) **アプリ 682 の権限**と **スペース参加者**は別設定。(2) **埋め込み**は初日は **一覧**を優先（グラフパーツだけだと空に見えやすい）。(3) **MoM** は前月実績が無いとき **「—」表示**でよい（§6.1）。

| 枠（目安・開始時刻は任意・前後可） | 担当 | 内容 |
|-----------------------------------|------|------|
| **ステップ1（約15分）** | **CIO**（API／MCP／ログ）＋**浜田**（ブラウザでしか分からない権限 UI のときのみ） | Space 48 **ポータル編集権限**の有無・**スレッド 52** 周辺の空きを確認（§2.2）。無ければ管理者依頼まで含める。 |
| **ステップ2（約30分）** | **CIO（実行）** | ポータル **編集**に相当する操作として **682 を埋め込み**（**アプリ表示／一覧**を推奨。§2.1）→**公開**まで。**公式 UI の手操作・Space REST（body）・Playwright 等**のいずれでも可（**§0**: Space 構成変更は **preflight・第2者・証跡**を欠かさない）。**浜田 CEO の手操作は必須ではない**（CEO は**確認のみ**）。 |
| **ステップ3（約30分）** | **CIO** | **当月合計**の置き場（標準「集計」パーツ or 682 内ビューへのリンク等）を **1 か所**決める。MoM は **前月データなしなら「—」** で仮置き（§6.1・§2.3）。 |
| **ステップ4（約20分）** | **浜田** | **目視確認**（依頼時・浜田責任）: ポータルから 682 へ **1 クリック**で辿れるか、二重計上に見える配置がないか（§2.3）。 |
| **ステップ5（約10分）** | CIO | **証跡**: `kintone-apps.md`／本 Runbook／`handoff-log.md` に **1 行**（配置方針・未決があれば次セッション項目）。 |

**役割の正（2026-05-11 CEO／CIO 整合）**: **実装＝CIO**（§3 と同旨）。旧表記「浜田（操作）＋CIO（指示）」は **誤解を招くため廃止**。

### 2.5 別ダッシュ専用アプリ＋`customize`（CEO 方針 2026-05-11）

**前提**: SPEC **`docs/plans/2026-05-08-user-support-daily-counts-spec.md` §6.1.1**。スペースは **Space 48**（決定済み）。**682 は入力正本のまま**。

| 順 | 内容 |
|----|------|
| 1 | **アプリ新規**（例: 名称「**ユーザサポート682ダッシュ**」）を **Space 48** に `kintone-add-app` 等で作成 → **`kintone-deploy-app`** で本番反映（**§41・台帳 `kintone-apps.md` 追記**）。 |
| 2 | リポに **`customize/<新appディレクトリ>/desktop.js`** を追加（**一覧は最小**で可・まずは **ポータル用の全画面 JS** 中心）。**682 からの REST 読取**で §6.2 集計を表示。 |
| 3 | **`npm run cio:preflight:<新スクリプト>`**（または既存 preflight パターンに合わせたゲート）→ **`npm run deploy:<新app>`** を `package.json` に **682 と同型で追加**（憲法 deploy 規律）。 |
| 4 | **682 側 7 暦月棒の撤去**（`SHOW_ROLLING_7M_ON_APP682=false`）は **ダッシュで同等が確認できてから**（§2.0 1 行目どおり）。 |
| 5 | **証跡 1 行**（本 Runbook §4 相当）＋**浜田 CEO 目視**（依頼時）。 |

**「明日すぐ作れるか」について**: 要件は SPEC に揃っているため **骨組み（埋め込み＋入口）は同日中に可能**な見込みが高い。**赤／青の MoM 表現**など kintone 標準だけでは足りない部分は **§6.1 どおり後続 customize** となり、**初日はプレースホルダーでよい**。

### 2.1 方針（CIO デフォルト）

| 項目 | 推奨 | 理由 |
|------|------|------|
| 682 との関係 | **アプリ 682 をポータルに埋め込み**（または公式の「アプリ表示」パーツ） | REST 単体ウィジェットより **実装コストが低い**。欠日バナー等は **682 一覧ヘッダ**で既に MVP 済み。 |
| MoM（前月比） | **前月の月合計が取れること**を確認 | 当月合計だけでは **MoM 計算不可**。前月レコードが無い月は **「前月比 —」** 等の表示でよい（§6.1 の文言は実装時に固定）。 |
| 二重計上 | **グラフは `day_total` のみ**（C と同じ） | SPEC §5・§6.2 と整合。 |

### 2.2 事前確認（権限・画面）

- [ ] **Space 48 のポータル／ダッシュボード編集権限**があるか（無ければ管理者に依頼）。
- [ ] **スレッド 52** 周辺の既存パーツと **レイアウトの衝突**がないか目視。

### 2.3 実装チェックリスト（最小）

1. [ ] Space 48 を開く → **ポータル**（またはダッシュボード）**編集**。
2. [ ] **682 アプリ**（一覧またはグラフ）を **配置**。
3. [ ] **当月合計**用に、**別パーツ**で集計・数式が必要なら kintone 標準の「集計」や **別ビュー**の利用可否を検討（**customize は §9.1 以降の拡張**で SPEC を更新してから）。
4. [ ] **MoM**: 前月データがあることを確認し、**赤＝増・青＝減**の表現は **標準で足りない部分**は後続 customize で対応（§6.1 明記）。
5. [ ] **二重表示**（同じ件数をグラフと表で二通りに足さない等）が無いか **§6.2 の精神**で確認。

**完了の定義**: Space 48 を開いた利用者が **682 の月次傾向と当日運用の入口**に **1 クリック以内**で辿り着ける。

---

## 3. AI チーム（マルチエージェント）での進め方

1. **DeepSeek**（着手前）: §5.1 と §6.1 の **食い違い・MoM データ条件・二重計上**の盲点 3 点＋突合メモ。
2. **CIO（本体）**: 682 UI 操作と Space 配置の **実行・証跡**（スクリーンショットは任意、**台帳・handoff の 1 行**は必須）。
3. **Kimi**（任意）: **長文 runbook 改訂**や **埋め込み HTML／JS 案**が 80 行超えるときにレビュー 1 問。
4. **OpenRouter**: DeepSeek 失敗時のフォールバック。

---

## 4. 完了報告に含める最小項目（浜田向け）

- **C**: グラフ名・公開有無・**確認した暦月**（例: 2026-04）。
- **D**: Space 48 の **編集 URL** または画面名・配置したパーツ一覧（短文）。
- **リスク・未了**: 権限不足・MoM 未実装など **次セッションに残すもの**を 1 行。
