# SKYSEA 段B — 回答前準備チェックリスト（2026-08-05）

**前提**: 段A 問い合わせ **送信済**（2026-08-05）。SKY社一次回答は **翌営業日まで**想定。  
**禁止**: 実PC配信・既存GPO変更・App 674 一括書込・旧594前提の `skysea-recon` 再実行。  
**正本**: `docs/runbooks/skysea-2026-08-ai-playbook.md` / plan §11.6–11.10

---

## B1. SKYSEA 全件CSV（浜田）

| 項目 | 内容 |
|------|------|
| 取得元 | SKYSEA管理機 → ハードウェア（またはクライアント）一覧 → **全件CSV** |
| 保管先 | **Git外**（推奨: `C:\tmp\skysea\` または社内保護フォルダ）。リポの `data/skysea/` は `.gitignore` 済だが元CSVは原則リポ外 |
| 命名 | `skysea-full-YYYY-MM-DD.csv`（送信日スナップショットを別名保持・上書き禁止） |
| AIが使う列 | PC名（ADコンピューター名）、導入状態、最終通信、クライアントバージョン 等の最小限 |
| 完了後 | 全台完了後 **3か月**で削除（§11.10） |

- [ ] CSVを取得した
- [ ] Gitに入れてない
- [ ] チャットまたは共有パスでAIへ提供した

---

## B2. App 674 突合ルール（AI・書込なし）

| 項目 | 固定値 |
|------|--------|
| 突合キー | **`pc_name` 完全一致のみ**（曖昧一致・自動修正なし） |
| 対象母集団 | 種別=**個人**、状態=**利用中**、既存PC、SKYSEA未導入 |
| 対象外 | 新規購入・初期化PC（手動導入継続）／ARM・旧Windows |
| 「導入済」確定 | 配信成功だけでは不可。**SKYSEA CSVに表示後**のみ（§11.10） |
| 台帳にないPC | 自動登録しない → **台帳未登録一覧**へ |

- [ ] キー=`pc_name` を関係者で再確認した（変更しない）
- [ ] CSV受領後、AIがドライラン表を作る（674は更新しない）

---

## B3. インストーラー共有フォルダー（浜田実施・AIは手順）

| 項目 | 内容 |
|------|------|
| 容量目安 | 約 **101MB** |
| 権限 | `Domain Computers` = **読取のみ**／管理者 = 書込可／**Everyone 広権限禁止** |
| 実行 | 共有から直接実行しない → **各PC一時フォルダへコピー** |
| 改ざん防止 | 配布前に正規ファイルの **SHA-256** を記録。PC側コピー後に一致確認。不一致なら全体停止 |
| 後片付け | 成否にかかわらず、必要ログ保存後にPC上の101MBを削除 |
| バージョン | 管理機 **17.210.07j** と対応しない旧インストーラーは配布しない（SKY回答で確定） |

- [ ] 配置パスを決めた（例: `\\<server>\skysea$\Client\`）
- [ ] 権限を設定した
- [ ] SHA-256 をメモした（回答・試験前に再計測可）

---

## B4. 専用ADセキュリティグループ（浜田実施）

| 項目 | 内容 |
|------|------|
| 方針 | **新規グループ**のみ。PCは通常OUから**移動しない** |
| 用途 | 承認済み配信対象PCだけをメンバにする |
| GPO | 新規専用GPOのセキュリティフィルタにこのグループを使う（**GO後**・既存GPOは触らない） |
| 操作 | グループ作成・メンバ追加は **浜田**。AIは手順・確認コマンドを提供 |

**グループ名案（確定は浜田）**: `SG-SKYSEA-Deploy-2026`（仮）

手順（概要）:
1. AD UC または PowerShell でセキュリティグループ作成（Global）
2. 試験時は **指定1台だけ**メンバ追加
3. 展開時はドライラン承認済み一覧だけ追加
4. 導入確認（SKYSEA CSV）後はメンバから外す（§11.12）

- [ ] グループ名を確定した
- [ ] 作成した（試験GO前でも先行作成可）
- [ ] メンバはまだ空、または試験PCのみ

---

## B5. 新規専用GPO案（設計のみ・作成はGO後）

| 項目 | 内容 |
|------|------|
| 既存GPO | **変更しない**（バックアップ必須としない） |
| 中身案 | SYSTEM権限のスケジュールタスクで、共有からコピー→SHA確認→サイレント実行（引数はSKY回答後に固定） |
| 適用 | 上記SGへのセキュリティフィルタのみ |
| `gpupdate /force` | **使わない**（通常更新待ち） |

- [ ] 設計メモのみ（本ファイルで足りる）
- [ ] **GPOオブジェクトの新規作成は実装GO後**

---

## B6. 回答待ちメモ

| 項目 | 値 |
|------|-----|
| 問い合わせ送信 | **2026-08-05**（ソフトウェアお問い合わせ完了） |
| 一次回答目安 | **翌営業日まで**（急ぎは電話） |
| 受信確認 | 入力メールへ確認メールあり — 浜田が受信確認 |

回答が届いたらチャットに貼る or 要約を共有 → AIが段C（試験1台）の実装GO可否を整理する。

---

## §50-3-8 突合（CIO）

- (a) CSVは送信時点スナップショットを別名保持・Git外 — DeepSeek指摘を採用
- (b) 674キーは **`pc_name`**（DeepSeekの「社員番号＋月」は本レーン非該当・不採用）
- (c) 回答前は本番書込・配信・既存GPO変更なし
