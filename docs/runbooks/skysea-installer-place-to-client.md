# SKYSEA — Client へインストーラー配置（手順）

**いつ**: 回答待ち中でも可（実実行・配信はしない）  
**配置先**: 配布用共有 `…\skysea$\Client\`（浜田作成済）  
**正本**: plan §11.6

---

## 手順（浜田）

1. **元ファイル**を用意（約101MB・管理機 17.210.07j 対応版。拡張子は `.msi` / `.exe` 等・SKY回答で最終確定）
2. **Client フォルダへコピーのみ**（共有上で実行しない・ダブルクリックインストール禁止）
3. 配置後、PowerShell で SHA-256 を記録:

```powershell
Get-FileHash -Algorithm SHA256 "\\Server01\共有フォルダ\skysea\Client\SKYSEADivSetupLite.exe"
```

4. ハッシュ文字列をメモ帳または下記メモ欄に残す
5. チャットに「配置完了」＋**実パス**＋**SHA-256**を1行共有（AIが正本へ転記）

---

## メモ欄（記入用）

| 項目 | 値 |
|------|-----|
| 配置日時 | |
| UNCパス | `\\Server01\共有フォルダ\skysea\Client`（2026-08-05 浜田確定） |
| ファイル名 | **`SKYSEADivSetupLite.exe`**（2026-08-05 浜田） |
| サイズ | （配置後に記入） |
| SHA-256 | （配置後に記入） |
| 管理機版との対応 | 17.210.07j（SKY回答で再確認） |

---

## やってはいけないこと

- Client 上でインストーラーを直接実行する
- Everyone 書込を付ける
- まだ PC へコピー／インストールする
