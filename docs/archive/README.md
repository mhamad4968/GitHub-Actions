# docs/archive/

**目的**: 3階層索引（`docs/constitution/00-rule-hierarchy.md`）に載らない **superseded** 文書の退避先。

- **現役プランの完了退避**は `docs/plans/_archive/`（`docs/plans/INDEX.md` 参照）。
- 本ディレクトリは **横断的なレガシー**用（必要時のみ追加）。

**検出**: `npm run verify:rule-hierarchy-prune`  
**退避**: `npm run cio:archive:rule-orphans`
