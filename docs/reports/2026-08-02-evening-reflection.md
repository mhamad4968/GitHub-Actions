# 夕反省 — 2026-08-02

> スコープ正本: `docs/runbooks/evening-reflection-scope.md`  
> **GO**: `docs/approved-changes/2026-08-02-evening-reflection-hamada-go.md`（浜田全承認）

## 1. 失敗（事実）

| # | 事実 |
|---|------|
| 1 | 下余白を間隔・棒高さ問題として反復修正したが、ユーザ PDF の多くは MediaBox **841×595（A4横）**。印刷ダイアログ向きが主因だった |
| 2 | PDF の MediaBox を初手で数値確認せず、見た目感想だけで deploy を重ねた |
| 3 | グラフ子要素 `height:100%` 先付けで棒スケールが誤判定停止した |
| 4 | 2枚目を `overflow:hidden`＋`break-inside:avoid-page` 共有で最終行（例: 2026/07/31）を切り落とした |
| 5 | 失敗ナレッジの MCP／RAG 化が浜田指摘後になり、同日中の再発防止に間に合わなかった |
| 6 | MCP に入れたあと **git runbook／WAKE からの参照経路が無く**、検索可能でも着手時に自動参照されない状態だった |

## 2. 改善案（ミス削減）— 行動

| ID | 内容 | 状態 |
|----|------|------|
| **A1** | 印刷の余白／見切れ指摘の初手は PDF MediaBox（縦≈595×842／横≈842×595）を確認してから CSS を触る | **GO** |
| **A2** | 向き依存の不具合は同じターンで Memory／RAG に1件残す（依頼を待たない） | **GO** |
| **A3** | 印刷レイアウトは見た目OKの前に、最終行日付・表末尾が切れないか1巡する | **GO** |

## 3. 改善案 — ルール・脚本

| ID | 内容 | 状態 |
|----|------|------|
| **S-PRINT-01** | 683 印刷受入: MediaBox＋向き＝縦を runbook に固定 | **GO・反映** |
| **S-PRINT-02** | 印刷グラフは `height:auto` 実測→棒スケール（`height:100%` 先付け禁止を runbook に明記） | **GO・反映** |
| **S-PRINT-03** | 一覧ページは overflow/avoid を page1 と共有しない | **GO・反映**（コード v24＋runbook） |

## 4. §体制・運用・MCP

| ID | 内容 | 状態 |
|----|------|------|
| **O-PRINT-01** | 印刷不具合は向き・縮小・MediaBox の3点切り分けを先に書く | **GO** |
| **M-RAG-01** | 重要失敗ナレッジは RAG だけでなく **git runbook に要約をミラー**する（MCP 単独は消える／参照漏れしうる） | **GO・反映** |
| **M-RAG-02** | 683 印刷作業の着手時は `query_documents`（MediaBox／印刷）または Memory `kintone-683-print-report` を1回読む | **GO** |
| **M-RAG-03** | Memory エンティティ名と RAG `source:` を runbook 末尾に固定記載する | **GO・反映** |

### 1-N 憲法運用レビュー（本日の結論）

- 第2者（DeepSeek／Kimi）は印刷修正で挟んだ。§50-3-8 スタンプあり。
- MCP: 格納はできたが **運用導線が弱く効果は受動的**（クエリしたときだけ効く）。M-RAG-01〜03 で git ミラー＋着手時参照を義務化。

憲法本文は変更なし。
