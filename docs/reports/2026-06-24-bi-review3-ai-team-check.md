# 業務改善 ver.02 — レビュー#3 AIチーム確認（2026-06-24）

## 確認結果

| 項目 | 判定 | コメント |
|------|------|----------|
| 未申請一覧（Q-GUIDE-11） | **GO** | `申請者`＋`作成者`のOR検索で下書きを捕捉。一時保存時に申請者セットで一覧精度向上 |
| 評価完了一覧（Q-GUIDE-12） | **GO** | 713 `締め済み`×完了日の会計年度キーでバッジ判定 |
| 提案番号（Q-NUM-01） | **GO** | 700に`提案番号`（unique）追加。初回申請時REST採番 |
| v1クローズ後の変更 | **GO** | 浜田指示（仕様→実装→確認依頼）で再開 |

## 技術メモ

- **採番競合**: 同時申請は稀。`like`検索＋最大値+1。重複時はAPIエラーで再試行可能
- **年次バッジ**: 完了日未設定レコードはバッジなし（年次集計と同じフォールバック方針）
- **デプロイ**: `npm run business-improvement:add-proposal-number` → `deploy:700` → `bundle:bi-guide` → `deploy:699`

## 浜田確認待ち

実装完了後、Space 5（699/700）で動作確認を依頼する。
