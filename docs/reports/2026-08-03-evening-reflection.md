# 夕反省 — 2026-08-03

> スコープ正本: `docs/runbooks/evening-reflection-scope.md`  
> **GO**: 2026-08-03 浜田すべて承認（`docs/approved-changes/2026-08-03-evening-reflection-hamada-go.md`）

## 1. 失敗（事実）

| # | 事実 |
|---|------|
| 1 | App719 社外禁止注記を、浜田が求めた「接続情報の印刷物」を超えて **一覧画面バナー＋一覧印刷**にも載せた。訂正まで余分な deploy が1回増えた |
| 2 | 初手で「画面／一覧印刷／拠点印刷」のどの面に出すかを §41 で固定せず、リンク先アプリ全体に広げて実装した |
| 3 | 報告下書きの □A1 表記ゆれで `cio:report-verify-response` が一度落ち、締め報告の自己検証を二度手間にした |

## 2. 改善案（ミス削減）— 行動

| ID | 内容 | 状態 |
|----|------|------|
| **A1** | 「接続情報」「印刷物」「一覧」など表示面が複数ある依頼は、編集前に **対象面を1行で固定**してから Diff する（曖昧なら印刷物優先で案を出し確認） | 承認済・本GO |
| **A2** | 注意書き・禁止文言の初回 deploy は **物理掲示に近い印刷面のみ**から始め、画面載せは明示依頼があるまでしない | 承認済・本GO |
| **A3** | 報告送信前は `cio:report-verify-response` を1回通し、□A1 は `ダブルチェック（誰と・結果）` 表記で固定する | 承認済・本GO |

## 3. 改善案 — ルール・脚本

| ID | 内容 | 状態 |
|----|------|------|
| **S-UI-SURF-01** | customize の「目立たせ文言」依頼用に、着手前チェック1行（画面／単票印刷／一覧印刷／Excel）を runbook または pre-implement 想起に足す | 反映済 |
| **S-REPORT-A1** | `cio:report-verify-response` 失敗時の典型誤記（□A1 に「誰と」括弧なし）を `docs/session-report-checklist.md` §P A1 に **正しい1行例**として固定 | 反映済 |
| **S-PRINT-FILTER-01** | 一覧印刷が「画面絞込＋別モーダル」の二重系になったときは、印刷ヘッダー要約に **適用フィルタ（画面／モーダル）** を必ず出す規則を dash 共通メモに1行 | 反映済 |

## 4. §体制・運用・MCP・憲法

| ID | 層 | 内容 | 状態 |
|----|----|------|------|
| **O-SURF-01** | 運用 | 「接続情報」「注意書き」依頼は、CIO が着手前に **表示面マトリクス（画面／単票印刷／一覧印刷／Excel）** を1行チャットへ出し、浜田確認または印刷優先の仮置きを明示する | 承認済・本GO |
| **O-CLOSED-01** | 運用 | `closed-v1` 台帳でも浜田の明示依頼があれば **微小 UI／印刷** は再開扱いでよい。完了サマリを GO待ちに出さない規律は維持し、closures 解除は大規模再開時のみ | 反映済 |
| **O-WAKE-01** | 運用 | WAKE 後の偽陽性 dirty は allowlist 追記（本日 knowledge-wake 済）を既定とし、残件は SESSION-CLOCK／翌日 pending 以外を締め前に0にする | 承認済・本GO |
| **M-5038-SURF** | MCP | §50-3-8 の短問に **表示面漏れ（画面／印刷／一覧）** を (a) の定型に含める（DeepSeek プロンプト1行追記） | 反映済 |
| **M-REPORT-01** | MCP／運用 | 報告下書きは IDE 手書きより **テンプレ生成→verify** を優先（表記ゆれで verify 落ちを減らす） | 反映済 |
| **C-41-SURF** | 憲法運用 | 多面 UI の表示範囲は §41 一問の典型題材と明記（GO 段階 G2 前でも「どこに出すか」だけは確認可）。AGENTS 本文は非置換・薄い runbook／18 追記で足りる | 反映済 |
| **C-GO-PRINT** | 憲法運用 | 印刷物向け文言は **確認A／調査** と混ぜず、実装GO後も **初回は印刷面のみ** を既定とする（A2 と連動。28-ceo-go は非置換） | 反映済 |

### 1-N 憲法運用レビュー（本日の結論）

- G0→実装GO（721 複数部署）は境界どおり機能した。
- 過大実装（719）は **表示面の §41 省略** が主因。憲法条文の不足というより **運用想起（面マトリクス）** の欠落。
- 憲法本文（AGENTS）改定は不要。薄い runbook／pre-implement 想起／§50-3-8 定型追記で足りる。
