【READ-01】新セッション初日 — やる順（CIO 用・2026-05-05 準備版）

**0) 引き継ぎ骨格（任意だが強く推奨）**  
　`chat-sessions/HANDOFF-AI-FIVE-BLOCKS.md` を **先に Read**（ブロック 1→5）。長文スターターと **同じ手順を 5 分割**した索引。

以下を **この順で** 実行し、各ブロックのあとチャットに短く残す。

1) 項番 -1  
   浜田が `Desktop\AI緊急用\00-NEW-SESSION-STARTER_yyyymmdd.txt`（**ハブ**）を貼ったあと、AI は `chat-sessions/session-starter-parts/part-A`〜`part-F` を **この順で** Read ツールで通読（offset 連鎖で抜けなし）。**ハブ単体**でも Part 通読は必須。

**§35-6（憲法・2026-05-04）**: **締め報告**は **`SESSION-CLOSE-REPORT_yyyymmdd.txt` を 1 本**（正本 `chat-sessions/`＋コミット、Desktop は sync 控え）。**`/mnt/c/.../AI緊急用` に直書き**したら **リポへ即反映＋コミット**のあと **`npm run session-starter:sync-desktop`**。副次リポ（例 `~/toto-prediction`）は **コピーだけで終わらせず `git status`→必要なら commit+push**。「古い」整理と **削除**は **浜田確認または §41 一問のあと**（`AGENTS.md` §35-6・**TSB-031**）。**DeepSeek 1 問を省略**したら **`§50-3-8 スキップ理由:`** 付きで **理由 1 行**（`AGENTS.md` §50-3-8 補足）。

1b) （任意）`chat-sessions/SESSION-CLOSE-REPORT_yyyymmdd.txt` があれば **項番 -0 前**に Read（無ければスキップ可）。

2) 項番 0（WAKE 標準 2026-06-21）  
   リポルートで **`npm run cio:session:cold-start`** → **exit 0**（内包: verify:constitution-handoff・凍結ゾーン・handoff テンプレ・bootstrap・import）。**L2**: cold-start NG 時のみ **`npm run session:bootstrap`** 単独。正本 **`docs/runbooks/session-lifecycle-v2.md`** §3。Desktop **`29-ABCD-V2-LIFECYCLE.txt`** 参照。

2b) **R-ORIENT-07（浜田 GO 2026-07-05）— 本日予定を先に**  
   bootstrap 完了後、**依頼を聞く前**に AI が **`736-july-2026-schedule.md`** 等から **本日の予定**（期限・直近3日・遅延 RED）を説明 → 末尾 **「以上です。本日の依頼をどうぞ」** → **その後** 項番 3。

3) 項番 -0  
   ティア宣言＋「次にやる **1 つ**」を **§41 一問だけ**浜田へ。OK が出るまで **本題に着手しない**（**2b の後**）。

4) 第0手（段階読解）  
   **本パック** `10-READ-02.txt` から **`15-READ-07.txt` までファイル名の番号昇順**で Read（中身が「（未使用）」のみならその番号はスキップ可＋チャットに理由 1 行）。**`14-READ-06.txt` を Read した直後**、**`28-CONSTITUTION-GENRE-MAP.txt` を Read**（ジャンル早見・4AI 役割・見落とし防止）し、チャットに **`【AI分業チェック】`** を **1 回必ず**出す（項目・表は READ-06 先頭のテンプレどおり。**省略・「同上」禁止**）。**`15-READ-07.txt`（浜田 CEO のお願い）**を Read した直後、チャットに **`【READ-07 読了】`** と **1 行**（「Project Rules 厳守・壁時計・分業・GH Actions・健全性を再確認」等、自分の言葉で要約）を残す。  
   続けて `chat-sessions/SESSION-READ-LADDER.md` を Read で **通読**。

5) A. 共通五段階（第1〜5段）  
   `SESSION-READ-LADDER.md` の表どおり。各段のあと **「事前準備用テンプレ」**（同ファイル内）。**ルール理解だけ**—仕様は書かない。

6) B. プロジェクト確認  
   項番 -0 の本題（5A / 5B）に合わせ B0 → B1 小出し。枝ごと **「プロジェクト確認用テンプレ」**。

7) 日終わり  
   `npm run session-starter:sync-desktop` → `npm run verify:desktop-ai-emergency-sync`（read-pack 含むバイト一致）。**壁時計停止**が指示されていれば **`npm run session:clock:clear`**（`session:clock:web` は Ctrl+C）。

正本の更新: この READ 番号ファイルは `chat-sessions/desktop-ai-emergency-read-pack/`。編集後は必ず上記 sync。
