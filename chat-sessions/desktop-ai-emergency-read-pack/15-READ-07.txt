【READ-07】浜田 CEO からのお願い（思い・**朝イチでも読む**）

> **正本**: 本ファイル（`chat-sessions/desktop-ai-emergency-read-pack/15-READ-07.txt`）。`npm run session-starter:sync-desktop` で Desktop `AI緊急用\15-READ-07.txt` に同名コピーされる。  
> 以前 Desktop にあった **`濱田からお願い（切実な・・・）.txt`** の思いは **本 READ-07 に統合**した（内容は下記）。

**Desktop `AI緊急用`（セッション切替で使う）**

- **パス**: `C:\Users\mhamada202408224\Desktop\AI緊急用\`（**セッション切替・新チャット開始時**に浜田が開き、**`00-NEW-SESSION-STARTER_yyyymmdd.txt`（ハブ）**を最初の 1 メッセージに貼る。**詳細 6 本**は **`01`〜`06`-STARTER-part-*.txt** で任意参照。同日のハブ正本は **日付 8 桁の 1 本**）。
- このフォルダの **09〜15-READ-…・08-INDEX・16-README-read-pack・17〜20（read-pack 末尾）** 等は **リポの `desktop-ai-emergency-read-pack/` と同期された控え**（**21〜23** は別リポ正本から同名コピー。順序は **`08-INDEX.txt`**）。**切替直後はチャット貼付＋必要ならここをメモ帳で開く** → CIO は **`chat-sessions/session-starter-parts/part-A`〜`part-F`** を **この順で** Read（スターター Part B「貼付単独で完走」節どおり。ハブのみ貼付でも可）。
- CIO は **Desktop だけ更新して放置しない**。**`npm run session-starter:sync-desktop`** は、正本（本 READ・`NEW-SESSION-STARTER.md`・`session-starter-parts/*.md`・`desktop-ai-emergency-read-pack/*.txt` 等）を変えた **同一ターンまたは直後のターンで CIO が自発実行**する（**浜田に「npm を実行して」と書かなくてよい**。チャットは **実行した事実 1 行**で足りる）。`/mnt/c` が無い等でスキップしたら **理由 1 行**。**可能なら**続けて **`npm run verify:desktop-ai-emergency-sync`**。依存順を揃えるなら **`npm run desktop:sync-and-verify`** を優先してよい（憲法 TSB-024）。
- **CIO 定常（マルチエージェント環境）**: **`npm run verify:cio-mcp-registry`**／必要なら **`npm run cio:mcp:env`**／週次の **`npm run mcp-status:refresh-usage`**（`docs/mcp-status.md` 更新まで）も **浜田に依頼せず CIO が実行**（`mcp.json` 変更後は **`npm run mcp:sync-cursor-windows`**）。**`cio:mcp:env` の日常正本は Windows ネイティブ**（`SUMMARY: OK 6/6`）；WSL `/mnt/c/...` は **月次ベストエフォート**／kimi のみ落ちるときは **`CIO_MCP_PROBE_KIMI_TIMEOUT_MS`** とネット（`docs/mcp-status.md` §CIO）。Desktop **`18-重要確認.txt`** の **CIO の自律判断**節と併読。
- **第12層（2026-06-14）**: customize 変更・deploy 前は **`npm run verify:kintone-live-schema -- --app <id>`**（Warning 0）。New Chat import 後は **`verify:git-history-alignment --handoff`** が **import 内で自動連鎖**。月次は **`npm run verify:kintone-live-schema -- --portfolio`**（`cio:periodic:monthly` 内包）。
- **毎回のメンテ時に正本と合わせる（2026-05-02 CEO 指示）**: **メンテ**＝`NEW-SESSION-STARTER.md`・`session-starter-parts/*.md`・read-pack・`HANDOFF-HUMAN.txt`・`SESSION-BOOTSTRAP-CHECKLIST.md`・`checkpoint-latest.md`・`handoff-log.md` など **Desktop 控えの元**をリポで触った**そのターンの締め**、**日終わり**、**push 直前**、**セッション締め**のいずれも含む。**`C:\Users\mhamada202408224\Desktop\AI緊急用\`** を **リポとバイト一致**まで更新する（上記 npm。**セッション切替時だけ**に限定しない）。

---

## 1. Project Rules と「理解しているか」

特定のフォルダに対してルールや知識を固定できる **Project Rules** は、**自動読み込み（alwaysApply / glob）でコンテキストに載る設定になっている**想定でよい。

ただし **「自動で載る＝条文をすべて暗記済み」とは限らない**。ルールに書かれたことは **厳守**し、**例外・知らなかった**は使わない。必要なら **該当 `.mdc` / `AGENTS.md` を Read で正本確認**する。

---

## 2. 絶対に守ってほしいこと（憲法・ルール）

対応するときは次を **例外なし**で守る。

- **憲法・ルールを厳守**（例外や「知らなかった」はない前提で動く）。
- **壁時計（2026-05-17 CEO）**: **Cursor を閉じる → 自動停止**（`sessionEnd` hook）。**Cursor を開く → 自動で set + watch + web 起動**（Windows は **ブラウザも自動で開く** ＋ チャットに URL）。手動停止: `npm run session:clock:stop`。Runbook: `docs/runbooks/session-clock-cursor-lifecycle.md`。

---

## 3. 🎖️ AI 内部の役割分担（2026-05-21 方式B / `AGENTS.md` §1-2-3-4）

「**開発=AI**」をさらに細分化。**CIO=Opus 4.8 デフォルト（軽量時のみ 4.7）**・**コード=Composer 2.5 Subagent（diff のみ）**・長文=Kimi・論理=DeepSeek。新セッション **1 ターン目**で **`[🎖️ 本セッション割当]`** 1 行必須（**Composer=** 含む）。正本: `mode-b-canonical.mdc` / `part-A-constitution-kernel.md` / `NEW-SESSION-STARTER.md`。

| 役割 | 担当 | 持ち場 |
|---|---|---|
| **CIO** | Opus **4.8 デフォルト** / 軽量時 4.7 | 判断・統合・規律 |
| **実務（コード）** | **Composer 2.5** Subagent | diff のみ・GO なし save/deploy 禁止 |
| **実務（長文）** | **Kimi** | 長文・`kimi_review` |
| **知恵袋** | **DeepSeek** | §50-3-8 |
| **バックアップ** | **OpenRouter** | 失敗時 |
| **浜田** | CEO | GO・目視 |

**画像 MCP 見送り**。内蔵 GenerateImage のみ → `assets/images/`（`cursor-generate-image-assets.mdc`）。

**補足**: 仕様確認 **知恵袋→CIO 突合** = **`.cursor/rules/deepseek-cursor-spec-division.mdc`**。

---

## 4. GitHub ワークフロー

**GitHub のワークフローでエラーが出ていたら速やかに直す**（大事）。**基本は AI 側で対応**を基本とする。

---

## 5. 依頼事項より「あなた（AI 側・実行環境）の健全さ」（＝**健康チェック正本**）

**ここでいう「健康」＝人間の睡眠・水分・就寝アドバイスではない。** **正**: **`npm run health-check`**（`scripts/health-check.mjs`）の **結果を事実で報告**する（正常／警告／skip の内訳・S9/S12/S15/S16・RAG documentCount 等）。**禁止**: 報告見出しを **「§1 心身の健康」** のようにし、**一般論のみ**で済ますこと。**禁止**: **`TSB-001`** を本節に結び付けること（`docs/troubleshooting.md` の **TSB-001 は fileKey 孤児ラベル**であり、実行環境の健康チェックとは無関係）。

**依頼事項より、実行環境の健全さ（エラー・歪み・ゲート結果）が大事**。妥協せず、**エラーや問題があれば速やかに対応**する。対応の最中でも **おかしいと思ったらこまめに確認**してよい。**浜田の承認は不要**（ただし憲法上 **Tier B / §52-8 等で GO が要る境界**は従来どおり）。

**hooks が IDE に届かない経路の二重化（至急）**: **報告・締め・GO 仰ぎ**は送信確定前に **`npm run cio:report-verify-response -- --file <下書き>`** で **exit 0**（CEO 全文行・§1 四行先頭・V2・□A1・禁止語を一括検査）。軽い確認のみなら **`npm run cio:chat-report-selfcheck`**。詳細は `CEO-MINIMUM-ABSOLUTE-BASELINE.txt`・`every-turn-rules-confirm.mdc`・`docs/session-report-checklist.md`。

---

## 6. ツール活用憲法・憲法適合の記録（2026-05-06 CIO 追補・**朝イチでも可**）

- **正本**: **`.cursor/rules/ai-agent-tools-constitution.mdc`**（`alwaysApply: false` + `globs: "**/*"`）。**CIO 体制の役割のまま**、MCP ツール選択・秘密禁止・**CIO 内での他AI協議（§0.5）**・**作業区切りの `[憲法適合]` 1 行（§7）**を固定した。
- **毎ターン先頭の 4 行**（`every-turn-rules-confirm.mdc`）は **そのまま**。**PR 前・deploy 前・一段落・完了前**に **`[憲法適合]`** で Tier・触れた §・第2者/MCP・§52-8 を **自己宣言**（詳細は同ファイル §7）。**憲法カード**（`constitution-brief-card.mdc`）にも 1 行追記済み。
- **変更履歴の控え**: Desktop 同フォルダの **`17-HISTORY-2026-05-06-read-pack-and-tools.txt`**（read-pack 正本と同名。`npm run session-starter:sync-desktop` でコピー）。
