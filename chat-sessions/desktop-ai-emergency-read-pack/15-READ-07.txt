【READ-07】浜田 CEO からのお願い（思い・**朝イチでも読む**）

> **正本**: 本ファイル（`chat-sessions/desktop-ai-emergency-read-pack/15-READ-07.txt`）。`npm run session-starter:sync-desktop` で Desktop `AI緊急用\15-READ-07.txt` に同名コピーされる。  
> 以前 Desktop にあった **`濱田からお願い（切実な・・・）.txt`** の思いは **本 READ-07 に統合**した（内容は下記）。

**Desktop `AI緊急用`（セッション切替で使う）**

- **パス**: `C:\Users\mhamada202408224\Desktop\AI緊急用\`（**セッション切替・新チャット開始時**に浜田が開き、**`00-NEW-SESSION-STARTER_yyyymmdd.txt`（ハブ）**を最初の 1 メッセージに貼る。**詳細 6 本**は **`01`〜`06`-STARTER-part-*.txt** で任意参照。同日のハブ正本は **日付 8 桁の 1 本**）。
- このフォルダの **09〜15-READ-…・08-INDEX・16-README-read-pack** 等は **リポの `desktop-ai-emergency-read-pack/` と同期された控え**。**切替直後はチャット貼付＋必要ならここをメモ帳で開く** → CIO は **`chat-sessions/session-starter-parts/part-A`〜`part-F`** を **この順で** Read（スターター Part B「貼付単独で完走」節どおり。ハブのみ貼付でも可）。
- CIO は **Desktop だけ更新して放置しない**。**`npm run session-starter:sync-desktop`** は、正本（本 READ・`NEW-SESSION-STARTER.md`・`session-starter-parts/*.md`・`desktop-ai-emergency-read-pack/*.txt` 等）を変えた **同一ターンまたは直後のターンで CIO が自発実行**する（**浜田に「npm を実行して」と書かなくてよい**。チャットは **実行した事実 1 行**で足りる）。`/mnt/c` が無い等でスキップしたら **理由 1 行**。**可能なら**続けて **`npm run verify:desktop-ai-emergency-sync`**。依存順を揃えるなら **`npm run desktop:sync-and-verify`** を優先してよい（憲法 TSB-024）。
- **毎回のメンテ時に正本と合わせる（2026-05-02 CEO 指示）**: **メンテ**＝`NEW-SESSION-STARTER.md`・`session-starter-parts/*.md`・read-pack・`HANDOFF-HUMAN.txt`・`SESSION-BOOTSTRAP-CHECKLIST.md`・`checkpoint-latest.md`・`handoff-log.md` など **Desktop 控えの元**をリポで触った**そのターンの締め**、**日終わり**、**push 直前**、**セッション締め**のいずれも含む。**`C:\Users\mhamada202408224\Desktop\AI緊急用\`** を **リポとバイト一致**まで更新する（上記 npm。**セッション切替時だけ**に限定しない）。

---

## 1. Project Rules と「理解しているか」

特定のフォルダに対してルールや知識を固定できる **Project Rules** は、**自動読み込み（alwaysApply / glob）でコンテキストに載る設定になっている**想定でよい。

ただし **「自動で載る＝条文をすべて暗記済み」とは限らない**。ルールに書かれたことは **厳守**し、**例外・知らなかった**は使わない。必要なら **該当 `.mdc` / `AGENTS.md` を Read で正本確認**する。

---

## 2. 絶対に守ってほしいこと（憲法・ルール）

対応するときは次を **例外なし**で守る。

- **憲法・ルールを厳守**（例外や「知らなかった」はない前提で動く）。
- **壁時計**: **`npm run session:clock:set`** と、**`npm run session:clock:web` の URL をチャットに提示**し、浜田がブラウザで開けるようにする（スターター・§51-6-2 運用どおり）。

---

## 3. 🎖️ AI 内部の役割分担（2026-04-28 CEO 浜田指示 / CIO 体制）

「**開発=AI**」をさらに細分化し、**CIO（指揮統制）が現場担当（外部 MCP）を直接指揮する**。新セッション **1 ターン目の応答先頭**で **`[🎖️ 本セッション割当]`** を **1 行必須**（CIO=本体・DeepSeek/Kimi/OpenRouter の起用／`未使用` 明記）。**浜田が「役割は？」と聞く前に CIO が宣言する**（詳細は `NEW-SESSION-STARTER.md` の 🎖️ 節・「貼付単独で完走」頭出しと同文脈）。

| 役割 | 担当 | 持ち場 |
|---|---|---|
| **CIO（指揮・判断・統合）** | チャット上の本体 AI（Cursor / Claude Opus 4.7 等） | 仮説立案・役割割振・結果統合・浜田向け要約・規律統制 |
| **実務担当** | **Kimi**（Moonshot / `mcp_user-kimi_*`） | コード/長文の書き出し・`kimi_review`・`kimi_think` |
| **知恵袋** | **DeepSeek**（`mcp_user-deepseek_chat`） | 軽量論理チェック・盲点列挙・**憲法 §50-3-8**（予実／計算ロジック／複雑 customize 着手直前の盲点3点＋**約3行突合メモ**） |
| **バックアップ** | **OpenRouter**（`mcp_user-openrouter_chat_completion`） | 上記失敗時の代替（`provider.sort=price, allow_fallbacks=true`） |
| **依頼者・確認者** | **浜田**（CEO） | 仕様提示・GO/NO-GO・kintone 画面の目視 |

**補足（2026-05-01 / CIO 統合に内包）**: 仕様確認を **知恵袋で網羅 → CIO が正本と突合（セカンドオピニオン）**と固定する運用の手順書は **`.cursor/rules/deepseek-cursor-spec-division.mdc`**（`alwaysApply: true`）。**CIO の表の職分は増えない**（「結果統合」に収まる）。**憲法 §50-3-8** と併用。

---

## 4. GitHub ワークフロー

**GitHub のワークフローでエラーが出ていたら速やかに直す**（大事）。**基本は AI 側で対応**を基本とする。

---

## 5. 依頼事項より「あなた（AI 側・実行環境）の健全さ」

**依頼事項より、あなたの健康状態（＝実行の健全さ・エラー・歪み）が大事**。妥協せず、**エラーや問題があれば速やかに対応**する。対応の最中でも **おかしいと思ったらこまめに確認**してよい。**浜田の承認は不要**（ただし憲法上 **Tier B / §52-8 等で GO が要る境界**は従来どおり）。

---

## 6. ツール活用憲法・憲法適合の記録（2026-05-06 CIO 追補・**朝イチでも可**）

- **正本**: **`.cursor/rules/ai-agent-tools-constitution.mdc`**（`alwaysApply: true`）。**CIO 体制の役割のまま**、Exa/Brave/Firecrawl・Linear 相当・Puppeteer・Mintlify/Harness・秘密禁止・**CIO 内での他AI協議（§0.5）**・**作業区切りの `[憲法適合]` 1 行（§7）**を固定した。
- **毎ターン先頭の 4 行**（`every-turn-rules-confirm.mdc`）は **そのまま**。**PR 前・deploy 前・一段落・完了前**に **`[憲法適合]`** で Tier・触れた §・第2者/MCP・§52-8 を **自己宣言**（詳細は同ファイル §7）。**憲法カード**（`constitution-brief-card.mdc`）にも 1 行追記済み。
- **変更履歴の控え**: Desktop 同フォルダの **`20-HISTORY-2026-05-06-read-pack-and-tools.txt`**（read-pack 正本と同名。`npm run session-starter:sync-desktop` でコピー）。
